# FRENEMIES ON THE ROAD — Diario delle decisioni
*Sviluppo del prototipo digitale · giugno 2026 · da leggere insieme a `frenemies_on_the_road_kb_v1_2.md` (regolamento) e `index.html` (codice)*

Questo documento registra le decisioni prese durante lo sviluppo del gioco digitale: chiarimenti di regole confluiti nella Knowledge Base (da v1.1 a v1.6), interpretazioni implementative dove il regolamento taceva, scelte tecniche e roadmap. È la fonte da consultare prima di modificare il codice o le regole.

- **Regola v1.44: le due rese fuse in un'unica "Concedere la scena"** (luglio 2026): richiesta di Saverio, nata dalla sensazione che "avere due meccaniche per la resa è ridondante". Fino alla v1.43 chi cedeva la scena (senza iniziativa, una sola carta, scene 1-4) sceglieva tra **Ritirata strategica** (carta rimescolata nel mazzo, 0 punti, munizioni per il finale) e **Resa onorevole** (1 punto, carta agli scarti). Ragionamento: la ritirata serviva a garantire munizioni per i colpi di scena, ma quel bisogno è già coperto (meglio e con più controllo) da **comprare figure** e **usare il Jolly**, quindi il riciclo era la variante ridondante. Valutate tre strade (tenere solo la resa; tenere solo la ritirata; fondere in una concessione più forte che dà punto **e** seppellisce): scelta la più snella, **una sola mossa "Concedere la scena"** = **1 punto** (come una presa), carta agli **scarti comuni**, una volta per lato. Effetto collaterale positivo: §8 perde un ingresso di carte nel mazzo (resta solo il recupero del Re). **Requisito UI aggiunto in corsa:** il testo della concessione lo si scrive **solo vs Claude** (modalità solo), non in PvP, dove si narra a voce al tavolo. **Bilanciamento:** cambio che tocca il gioco (concedere ora fa sempre 1 punto, prima la ritirata dava 0) → benchmark da rilanciare; da tenere d'occhio che concedere non diventi troppo conveniente. **Propagazione (stesso commit):** motore `index.html` (`G.concessione`/`G.concessioneScena`, helper `concessioneDisponibile`/`eseguiConcessione`/`chiediConcessione`, bottone unico `btnConcedi`, `rec.concessaDa`, banner/tracker, ramo IA con JSON `{concedi}`; rimossi tutti gli identificatori `ritirata*`/`RESA_NOME`), `REGOLE` (§14, §18), `REGOLE_IA`, cross-ref sacrificio figura (§15.3); KB §18.1 + §8 + §14 + §15.3 + registro + footer v1.44; manuale + glossario + footer; driver `test/partita_smart.js` e `test_ritirata.js` (mossa unica) + dismiss dei driver core. **Collaudo:** suite core verde. **Benchmark:** rilanciato, `storico.jsonl` + `ultimo.md` committati.
- **Regola v1.43: il Fante cambia effetto (spia e riordina un mazzo), via tutte le eccezioni** (luglio 2026): richiesta di Saverio, nata dalla sensazione che il Fante fosse "troppo complicato, con troppe eccezioni" (mano vuota, mazzo vuoto in scena 5). Ragionamento fatto insieme, passo per passo: (1) la radice di tutte le eccezioni era che l'effetto toccava **mazzo e mano**; (2) valutata la "soffiata" (guardare la mano avversaria), scartata perché **muore per tempismo** — la mano si svuota a ogni scena (KB §353: si giocano 4 carte, a fine round la mano è vuota), quindi l'informazione è inutile se giochi il Fante per ultimo; (3) valutato l'effetto in scena 5 sulla riserva avversaria, scartato perché **spoilera i colpi di scena** (§24, il duello è fatto di rivelazioni). Effetto scelto: **spii le prime 3 carte di un mazzo a scelta** (tuo o avversario) e decidi **quali lasciare in cima e quali mandare in fondo**, guardandole soltanto (nessuna carta si muove). Il valore si realizza alla **prossima pescata**, quindi conta anche giocando il Fante per ultimo; in scena 5 (nessun mazzo) **non ha effetto**, confine naturale, non un'eccezione. Finestra utile scene 1-3 (in scena 4 la rivelazione della mano estesa, §23.1, azzera l'ordine del mazzo). **Nerf accettato di proposito:** il Fante è la figura più economica (3), va comprata e usata presto. **Propagazione (stesso commit):** motore `index.html` (nuovo helper `effettoFanteSpia` con scelta mazzo `fanteMazzoMio`/`fanteMazzoAvv` + modale di riordino `fanteReordOk` + ramo IA; ramo Fante di `effettoFigura` ridotto alla chiamata; **rimosso** `effettoFanteRiserva`), costante `FIGURE`, tabella `REGOLE` + esempio `.es`, `REGOLE_IA`, prompt del mercato; KB §21 + esempio §21.1 + registro + footer v1.43; manuale §5 + glossario + footer; driver `test/partita_smart.js` (handler dei nuovi modali + metriche del Fante) e i driver di collaudo. **Collaudo:** suite core verde. **Benchmark:** rilanciato (cambio che tocca il gioco), `storico.jsonl` + `ultimo.md` committati.
- **Regola v1.43 (Regina): rimpiazzo sempre dagli scarti, scelto da chi gioca** (luglio 2026): stessa sessione e stesso spirito del Fante (sopra), su richiesta di Saverio. Fino alla v1.42 la Regina faceva scartare all'avversario una carta a caso e la rimpiazzava con la **prima del suo mazzo** (scene 1-4), o **a caso dalla riserva** (scena 5), o **dagli scarti** solo come ultimo ripiego: stessa struttura a eccezioni del vecchio Fante. Dalla v1.43 il rimpiazzo viene **sempre** da una carta **bassa scelta da chi gioca la Regina** negli **scarti comuni** (percorso unico, ogni scena). Effetti voluti: **(a)** declassamento **garantito** (prima il pescato dal mazzo era casuale e a volte migliorava la mano nemica) → lieve **buff** alla Regina, accettato e da misurare col benchmark, reversibile (se troppo, far scegliere la carta all'avversario); **(b)** la Regina **non tocca più il mazzo** dell'avversario (richiesta esplicita di Saverio: "al contempo non toglie più una carta dal mazzo"). Fallback unico: scarti vuoti → una carta in meno. Implementazione: riusa `reginaDaScarti`/`reginaScartiOk` (nato per il caso scena 5 in v1.42), promosso a percorso unico; rimossi i rami mazzo/riserva dal ramo `D` di `effettoFigura`. **Propagazione:** stessa lista del punto Fante v1.43 (motore, `FIGURE`, `REGOLE`+esempio, `REGOLE_IA`, prompt mercato, KB §21 + registro + footer v1.43, manuale §5 + glossario, driver di collaudo con dismiss di `reginaScartiOk`). **Collaudo:** suite core verde. **Benchmark:** rilanciato insieme al Fante.
- **Regola v1.42: Fante e Regina hanno effetti dedicati nei casi limite dell'ultima scena** (luglio 2026): richiesta di Saverio, nata dal manuale (aveva scritto a mano come dovevano funzionare in scena 5). Fino alla v1.41 in scena 5, col mazzo esaurito, il **Fante** non faceva nulla (spesa sprecata) e la **Regina** ripescava "la prima" carta della riserva o, senza mazzo né riserva, lasciava l'avversario "con una carta in meno". Nuove regole: **Fante** in scena 5 sbircia la **riserva avversaria** e chi lo gioca **sceglie** una carta da toglierle (torna nella mano nemica); l'avversario **rimette in riserva** la sua carta di mano più bassa (automatizzato: la scelta razionale che cede). **Regina** in scena 5: il rimpiazzo dalla riserva è **a caso** (non più "la prima"); se l'avversario non ha né mazzo né riserva, è costretto a **raccogliere dagli scarti comuni** una carta **scelta da chi ha giocato la Regina** (che gli rifila la peggiore); solo con scarti vuoti resta con una carta in meno. **Interpretazione fissata con Saverio (via domanda):** per il Fante sceglie *chi lo gioca* quale carta togliere dalla riserva (lettura forte, coerente con «costringerlo»), mentre il ritorno in riserva dell'avversario è automatizzato con la carta più bassa; nessun secondo modale rivolto all'avversario (coerente con l'architettura: gli effetti si risolvono per l'attore, il lato avversario si auto-risolve come già per la Regina base). **Propagazione (stesso commit):** motore `index.html` (`effettoFigura` ramo Fante → helper `effettoFanteRiserva` con modale `fanteRisOk` + ramo IA; ramo Regina → rimpiazzo riserva a caso + helper `reginaDaScarti` con modale `reginaScartiOk` + ramo IA), costante `FIGURE` (`eff` Fante/Regina), tabella `REGOLE`, `REGOLE_IA`, prompt del mercato; KB §21 + registro + footer v1.42; manuale §5; driver `test/partita_smart.js` (handler dei due nuovi modali + categorizzazione seed `fanteRiserva`). **Collaudo:** suite core verde. **Benchmark:** da rilanciare (gli effetti pesano su riserva e duello in scena 5). L'esempio §34/Appendice B resta obsoleto.
- **Manuale: rinominato il blocco del diagramma "flusso della partita"** (luglio 2026): richiesta di Saverio, coda della v1.40. Nel diagramma `flusso-partita` (Cap. 1, generato da `docs/genera_pdf_manuale.py`) il penultimo blocco si chiamava ancora **"Lettura del finale e domande"**, nomenclatura pre-v1.40 (che aveva rimosso le domande di fine partita e reso la Crescita una misura a parte, letta a scaglioni). Rinominato in **"Valutazione della Crescita"**, coerente con il nuovo finale (§26 KB, Cap. 7 manuale): dopo il duello dei colpi di scena la missione è già decisa e si legge solo la Crescita. Solo il generatore del PDF; nessun cambio di regole né di codice di gioco. Il PDF del manuale si rigenera dal solo KB/generatore alle milestone.
- **Regola v1.41: tetto al recupero del Re — valore massimo 10 meno le scene vinte dall'avversario** (luglio 2026): richiesta di Saverio. Fino alla v1.40 il Re (§21) recuperava dagli scarti comuni una qualsiasi carta **numerica** del proprio seme, senza limite di valore. Nuova regola: la carta recuperata deve avere **valore al massimo `10 - scene già vinte dall'avversario`** (10 è il valore del Re; contano solo le scene **già risolte** prima di quella in corso, i pareggi no). Il tetto cala da 10 (avversario a 0 scene) fino a 6 (avversario a 4 scene): morde progressivamente su 7-8-9-10. **Motivazione:** nerf mirato a spezzare la strategia dominante «compro il Re all'ultimo mercato, lo gioco in scena 5, recupero il 10 e lo metto in riserva» che garantiva un colpo di scena praticamente imbattibile nel duello finale (§24), e il possibile **stallo** se entrambi i lati la giocano; il tetto morde esattamente lì (in scena 5, con l'avversario a ≥2 scene vinte, il 10 diventa irraggiungibile). Interpretazione fissata con Saverio: le scene vinte sono quelle **già risolte** (dato disponibile a runtime da `G.scene[i].vincitore`, `"P"`/`"O"`/`"parita"`), i pareggi non contano; se negli scarti non c'è alcuna numerica del seme entro il tetto l'effetto non fa nulla (come già quando manca il seme). Tutto il resto del Re invariato. **Propagazione (stesso commit):** motore `index.html` (`effettoFigura` ramo Re: `scenePerseAvv`/`tetto = 10 - scenePerseAvv`, filtro `val <= tetto`, ramo IA e modale), `REGOLE` (campo `eff` del Re + tabella figure), `REGOLE_IA` e prompt del mercato, KB §21 + registro + footer v1.41, manuale §5, questo diario. **Benchmark:** da rilanciare (`test/benchmark.js`) e committare `storico.jsonl`/`ultimo.md`. **Collaudo:** suite core verde (`test_solo`, `test_partita` NG2/NG4). L'esempio §34/Appendice B resta obsoleto (da rifare con nuovo seed-vetrina).
- **Regola v1.40: il finale non incrocia più Missione e Crescita — Crescita a scaglioni, via i quattro esiti e le domande** (luglio 2026): richiesta di Saverio. Fino alla v1.39 il finale si leggeva su **due piani** che si **incrociavano** in una tabella di **quattro esiti nominati** (Vittoria piena / Per il rotto della cuffia / Sconfitta dignitosa / Fallimento totale), seguita dalle **domande di fine partita**. Nuova impostazione: la missione è **solo compiuta o fallita** (l'hanno già decisa la scena 5 e il duello dei colpi di scena, e il gioco ha già raccontato *come*: non serve una tabella a qualificare il tipo di vittoria), e la **Crescita** diventa una **misura a parte** che **non si incrocia** con la missione. La si legge **a scaglioni**, uno per lato, e la si tiene presente per l'**epilogo** (è il primo materiale da cui attingere per le vignette). Rimosse tabella dei quattro esiti, nota «Parità ai punti» e domande di fine partita; resta la lettura simmetrica (missione fallita dai Protagonisti = compiuta dall'Opposizione). **Soglie decise coi dati:** partiti dalla proposta di Saverio (0-1 / 2-3 / 4+), ma la distribuzione reale della Crescita nel benchmark (250 partite, 500 lati: mediana 6, quartili 4 e 8) mostrava che con 4+ la fascia alta scattava nell'80-85% dei casi (inutile come distinzione). Provate 0-3/4-7/8+, 0-2/3-4/5+, 0-3/4-6/7+; Saverio ha scelto **0-2 / 3-6 / 7+** (bassa P 6% / O 11%, media P 50% / O 59%, alta P 44% / O 30%). Etichette — Protagonisti: Nessuna crescita / Crescita / Grande crescita; Opposizione (stesso numero, l'idra): Minaccia spenta / Minaccia intatta / L'idra. **Propagazione (stesso commit):** `index.html` (`rFinale` — rimosso il cartellone dei quattro esiti nominati, ora intestazione **missione compiuta/fallita** dal duello + nuovo helper `scaglioneCrescita` e due pannelli Crescita, uno per lato; restano i pannelli di colore «L'ultima scena» e «Il rapporto»; `REGOLE` blocco finale, `REGOLE_IA`), KB §26 riscritto (+ §25 nota, Appendice B in breve, registro e footer v1.40), manuale (cap. 7 riscritto: §7.1 Crescita a scaglioni, §7.2 epilogo; rimossi vecchi §7.1 due piani / §7.3 quattro esiti / §7.4 domande; più §1.9, indice, flusso §1.10, coda §6.3, glossario «Crescita»/«Epilogo», rimossa voce «Quattro esiti», e finale dell'actual play Appendice B — Paola 7 = grande crescita, Omar 2 = minaccia spenta), questo diario. **Benchmark** `test/benchmark.js`: la metrica di vittoria passa da `r.outcome` (la stringa dell'esito, non più esistente) a `r.missione` (vincitore del duello); la voce «Esiti» (i quattro nominati) è sostituita dalla **distribuzione della Crescita per scaglione**. La **simulazione è identica** (nessuna meccanica cambia: missione, punti e mercato invariati), muta solo la lettura del finale; la vecchia riga «Esiti» non è più confrontabile. **Collaudo:** suite core verde. *(Realizza la direzione già anticipata nel diario alla v1.33: «la Crescita diventerà solo colore narrativo delle vignette e la missione resterà decisa dalla scena 5 dopo i colpi».)*
- **Regola v1.39: il Jolly dell'Opposizione — la carta ceduta si scarta (piccola asimmetria)** (luglio 2026): idea di Saverio. Fino alla v1.38 afferrare il Jolly era **simmetrico**: entrambi i lati seppellivano una carta della mano in fondo al proprio mazzo (che così tornava in gioco — anzi, in scena 5 alimentava la riserva: un vantaggio, non un costo). Ma i due lati non sono simmetrici: P, usando il Jolly, **arma O** (glielo passa), mentre O è l'**ultimo detentore** e non arma nessuno → per O era tutto vantaggio. Nuova regola: quando **O** afferra il Jolly, la carta ceduta la **scarta** (va negli scarti comuni) invece di seppellirla; il suo mazzo **cala di 1**, cioè una carta in meno verso la riserva dei colpi di scena. P resta invariato (seppellisce). Due punti chiariti con Saverio: (1) la carta di O va **negli scarti comuni** (non fuori dal gioco); (2) O usa il Jolly **quando gli conviene per vincere una posta** (uso attivo, come già faceva: cattura la carta avversaria più alta), non lo si rende conservativo come P. **Misura:** provata prima con O reso conservativo *e* costo → P 56,4 (sovra-corregge); poi con O attivo + solo il costo → **P 52,4 / O 47,6** (dal 48/52 della v1.38): il costo sposta il leggero vantaggio da O a P. Saverio **preferisce il leggero sbilanciamento a favore di P** (è la loro storia). **Propagazione (stesso commit):** motore `index.html` (`avviaUsaJolly` ramo O → `scarta(x)`; `eseguiJollyIA` idem; banner di `opzioniJolly` e `statoPerIA`/`REGOLE_IA`/`REGOLE` — corretto anche un residuo: `statoPerIA` descriveva ancora la vecchia spinta-Jolly rimossa in v1.37), KB §22.4 + registro + footer v1.39, manuale §5.4 + glossario + footer. Driver `test/partita_smart.js`: policy del Jolly di O invariata nello spirito (uso attivo per vincere la posta), lo scarto lo applica il motore. **Collaudo:** suite core verde. *Nota:* baseline benchmark aggiornata a v1.39 (non confrontabile con ≤ v1.38: cambia il costo del Jolly di O).
- **Regola v1.38: i colpi di scena diventano un duello a rilancio; le figure escono dal gioco** (luglio 2026): lungo ragionamento di design con Saverio. Problema di partenza: se nei colpi di scena "tutti buttano nel piatto tutta la riserva", l'ultima scena è ininfluente e priva di dramma. **Idea di Saverio (adottata):** finito il primo conteggio il piatto si svuota; chi ha vinto il conteggio **difende** dietro un **muro = numero di scene vinte** (l'ultima vale +1); l'altro **sfida** e apre calando dalla riserva una carta **≥ muro**; poi a turni alterni si prende la testa con una carta **≥** a quella in cima (**pareggiare ribalta**, così nemmeno un 10 è sicuro), e vince la missione l'ultimo che riesce a calare. Sono rimosse eliminazione e vincolo di seme (v1.3/v1.28): contano solo i valori. Scelte fissate con Saverio: comparatore **≥** (misurato più bilanciato del **>**, che dava vantaggio al difensore ~1,6%); il **muro è le scene vinte**, non la Crescita (le carte arrivano a 10, la Crescita a 5-8 sarebbe fuori scala). **§25 — la missione la decide il duello**, non più il piatto (rimosso il tie-break Crescita v1.29). In caso di **pareggio nel primo conteggio**, la **facoltà**: chi ha più Crescita (a pari, chi ha l'iniziativa) può **azzerarla per vincere il conteggio e difendere**, oppure rinunciare cedendo la scena all'altro; se nessuno esercita, va all'Opposizione. **§23 — in riserva solo carte numeriche** (le figure restano in mano); la scelta della mano finale (alte in mano per il conteggio o in riserva per il duello) diventa strategica. **§16/§21 — le figure escono dal gioco quando usate** (non negli scarti), quindi il **Re** recupera solo carte **numeriche**; nell'ultima scena il recupero del Re è forte (una carta alta in riserva, decisiva nel duello). **Indagine sul benchmark (con Saverio):** i primi numeri davano 41/59 e riserve ~0; scavando si è trovato un **bug del driver del mercato** — `buy()` apriva il modale PNG per P e poi per O nello stesso passo, e l'unico overlay veniva sovrascritto: gli acquisti di P non si finalizzavano (contatori gonfiati, P svantaggiato). Corretto (finalizzazione inline del modale, conteggio solo se l'acquisto va a segno); ora **coerenza driver↔motore 0 mismatch su 250**. Con il driver corretto le riserve risultano **~1,5-2 carte** (come previsto da Saverio) e i ribaltoni **simmetrici per-sfida** (~50% ciascuno): l'asimmetria vista prima era l'artefatto del bug. **Policy del benchmark** cambiata su richiesta di Saverio: compra la figura **più economica** disponibile (massimizza le figure); mano finale **bilanciata** (in riserva una media per aprire + una alta per vincere). **Bilanciamento (nuova baseline, non confrontabile con ≤ v1.37):** missione **P 48 / O 52**, ribaltoni **~51%** delle partite (pro-P 20%, pro-O 31% — differenza dovuta a P che difende più spesso, non a un vantaggio nel duello), riserva ~1,5-2 carte, Crescita netta P 6,4 / O 5,3. **Propagazione (stesso commit):** motore `index.html` (`avviaColpi`/`rColpi`/`eseguiColpo`/`prossimoColpo`, comparatore `COLPO_RIBALTA`, fase `pareggio_finale` con `rPareggioFinale`/`risolviPareggio`, `conteggioDefinitivo` che fissa `G.scene[4].missione`, `rFinale` con esito dal duello, helper `scarta()` con `G.fuoriGioco`, guardia del Re, `iaColpi` a rilancio, prompt mano finale), `REGOLE`/`REGOLE_IA`, KB §16/§21/§23/§24/§25 + registro + footer v1.38, manuale, driver `test/partita_smart.js` (mano finale bilanciata, colpi a rilancio, pareggio, metrica ribaltone, fix mercato) e i driver di collaudo (`test_partita`/`test_solo`/`test_riprova`/`test_ritirata` gestiscono `pareggio_finale` e il nuovo `colpi`). *Collaudo:* suite core verde. Nota: figure in `colpi` non più possibili (riserva numerica), quindi le metriche `reginaColpo`/`reColpo` del benchmark restano a 0 (l'idea di "figura che entra in cripta nei colpi" non esiste più). L'esempio §34/Appendice B resta obsoleto (da rifare con nuovo seed-vetrina).
- **Regola v1.37: il Jolly meno potente e simmetrico (cattura di una sola carta, per entrambi i lati)** (luglio 2026): lungo ragionamento di design con Saverio. Obiettivo: rendere il Jolly **meno potente** e **più simmetrico**. Fino alla v1.36 il Jolly era asimmetrico: P lo usava come **cattura elastica di gruppo** (poteva spazzare più carte / fare scopa facilmente), O come **spinta extra** (presa→scopa) che quasi non scattava mai (0.02/partita). Nuova regola: il Jolly ha valore elastico ma cattura **una sola carta** del piatto (scopa solo se era l'unica rimasta), e **entrambi i lati lo usano allo stesso modo** (cattura); la **spinta-Jolly è rimossa**. Restano invariati: Jolly unico che passa P→O, finestre P 1-3 → O 2-4 (O solo in scena successiva a P, mai due usi nella stessa scena), si afferra seppellendo una carta in fondo al mazzo, 0 punti, +2 Crescita se non usato, escluso dall'asta, il tema del peccato che emerge. Chiarimento di Saverio: **seppellire non è un costo ma un vantaggio** (la carta rientra in mazzo/riserva, il mazzo cresce), quindi il Jolly a cattura singola resta appetibile — in particolare nella situazione da resa (invece di arrenderti, seppellisci la carta bassa, afferri il Jolly e catturi la carta avversaria decisiva per ribaltare la scena). **Propagazione (stesso commit):** motore `index.html` (`jollyUsabile` esteso a O con finestre + solo dopo il passaggio; `opzioniJolly` a cattura singola; rimossi `jollySpintaDisponibile`/`spintaJolly`/il bottone in narrazione/il ramo in `iaNarrazione`; l'IA di O usa la cattura via `iaJollyPickO`/`eseguiJollyIA` in `iaTurno`), `REGOLE`/`REGOLE_IA`, KB §22.2/§22.4 + registro/footer v1.37, manuale §5.1/§5.4 + glossario + footer, driver `test/partita_smart.js` (`jollyPick` a cattura singola) e `benchmark.js` (etichetta). **Politica d'uso del Jolly ASIMMETRICA (decisa con Saverio):** P resta **selettivo** (usarlo arma O: solo alla chiusura, per ribaltare una scena persa), mentre **O lo usa appena può** (è l'ultimo detentore, non arma nessuno, e seppellire cresce la sua riserva): cattura la carta avversaria più alta, tranne se nel piatto ci sono solo carte proprie o se può già fare una scopa normale. Applicata sia nel driver (`jollyPick` per O) sia nell'IA vs Claude (`iaJollyPickO`/`eseguiJollyIA` in `iaTurno`). **Bilanciamento:** con O che usa il Jolly realisticamente, missione **P 50.0% / O 50.0%** e uso del Jolly **simmetrico** (cattura P 0.22 · O 0.22). Il Δ rispetto alla v1.36 (57.2%→50.0%) somma due effetti: il Jolly più debole (single-card) e la nuova policy aggressiva di O. Nota: la policy d'uso del Jolly è cambiata, quindi le metriche Jolly non sono confrontabili con i run v1.32-v1.36 (che usavano O solo alla chiusura); da qui in poi è la nuova baseline. Insight registrato in memoria: il benchmark misura il gioco ottimale, e prima sotto-stimava O perché non valorizzava né la narrazione (far emergere il peccato) né il vantaggio del seppellire (riserva in scena 5). *Collaudo:* suite core verde.
- **Regola v1.36: niente spinta su una presa di figura** (luglio 2026): richiesta di Saverio. La spinta del pitch (e specularmente la spinta-Jolly dell'Opposizione) trasforma in scopa una presa fatta con una **carta numerica**; una presa fatta con una **figura** (Fante, Regina, Re) **non è più promuovibile a scopa**. Motivo: la figura è già forte (cattura elastica fino a 8/9/10, spesso su un gruppo di carte); darle anche la scopa via spinta la renderebbe sproporzionata. **Propagazione (stesso commit):** motore `index.html` (in `giocaCarta` la presa imposta `G.narr.boostCard` solo se `!c.jolly && !c.fig`, così nessuna spinta — pitch o Jolly, entrambe agganciate a `boostCard` — la aggancia), `REGOLE`/`REGOLE_IA`, KB (§9.8 con la quinta condizione, §22, registro, footer v1.36), manuale (§3.10, glossario, footer v1.36). I driver di test usano le spinte in modo difensivo (controllano i pulsanti), quindi nessuna modifica; il benchmark è stato ri-misurato perché il cambio tocca il gioco (prima una presa di figura poteva diventare scopa via spinta). *Collaudo:* suite core verde.
- **Regola v1.35: una sola figura per mercato per lato** (luglio 2026): richiesta di Saverio, diretta conseguenza della v1.34. Da quando le figure entrano in mano ed entrano in scena la scena dopo, comprarne più d'una nello stesso mercato affollerebbe la scena successiva di PNG (con abbastanza punti, fino a una mano di sole figure). Nuovo tetto: **una figura per mercato** per lato, in aggiunta al vincolo invariato di una sola per tipo in tutta la partita. Comprato l'acquisto, il mercato è chiuso agli altri acquisti per quel lato fino al mercato dopo; le figure si distribuiscono così una per scena. **Propagazione (stesso commit):** motore `index.html` (nuovo flag per-mercato `G.figMercato`, azzerato in `avviaMercato`, alzato in `acquistaFigura`, controllato nel click del mercato e nel render dei pulsanti; `iaMercato` compra al massimo una figura, nuovo formato JSON `{"acquisto": n|null}`), `REGOLE`/`REGOLE_IA`/testo del mercato in-app, KB (§19, §20, registro, footer v1.35), manuale (§4.1/§4.2, glossario, footer v1.35). **Bilanciamento:** la policy greedy del benchmark comprava già una sola figura per mercato, quindi il benchmark resta **invariato** (Δ ≈ 0): il tetto formalizza come regola ciò che il gioco ottimale già faceva. *Collaudo:* suite core verde.
- **Regola v1.34: le figure comprate entrano in MANO, non nel mazzo** (luglio 2026): richiesta di Saverio, parallela nel principio alla v1.33 (una risorsa che entra nella fiction quando la autori, non mescolata alla cieca nel mazzo). Fino alla v1.15 la figura comprata al mercato **entrava nel mazzo**, che poi si **rimescolava**, e la figura era quindi una pescata futura e casuale (poteva anche non uscire mai). Ora la figura, creata subito come PNG al mercato (già così), **entra direttamente nella mano** e va **in scena nella scena immediatamente successiva** all'acquisto. Conseguenze: (a) **niente più rimescolo** al mercato — nuovo ordine MERCATO (in mano) → pesca fino a 4; (b) **costo autoregolato** — la figura occupa uno slot mano, quindi peschi 3 carte base invece di 4 (il mazzo resta neutro, una base ci resta); (c) **informazione parziale voluta** — l'avversario sa che hai comprato una figura e che entrerà la scena dopo: è diegetico (crei un PNG con nome nella fiction condivisa) e alimenta la tensione dell'asta; (d) **asta = solo carte numeriche** — come il Jolly, anche le figure non si possono puntare nell'asta d'inizio scena, così una figura non viene mai "sprecata" coperta e ottiene sempre il suo momento in scena. Restano invariati: una figura per tipo per lato; gli effetti (Fante sbircia/scambia, Regina scarto avversario, Re recupero) scattano quando **giochi** la carta. **Scena 5:** la figura comprata all'ultimo mercato è già in mano ed è forzata tra le carte giocabili (mai in riserva); si scelgono solo `4 - (carte già in mano)` carte dal mazzo, il resto va in riserva. **Bilanciamento** (benchmark 250 seed, policy di mercato invariata quindi Δ = effetto della regola): missione P **51.2%** (da 53.6% della v1.33), leggermente più bilanciato; le figure ora entrano sempre in gioco (punti lordi P +2.52), il prezzo di mercato + lo slot mano bastano come costo. **Propagazione (stesso commit):** motore `index.html` (`acquistaFigura` → `L.mano.push`; rimossi `rimescolaSeComprato` e `G.compratoMercato`; `avviaMercato`/`avanzaScena` senza rimescolo; `rManoEstesa` forza le figure tra le giocabili; asta `rAsta`/`iaAsta` esclude `c.jolly||c.fig`), `REGOLE`/`REGOLE_IA`/prompt `iaMercato`, KB §mercato/§8/asta/registro/footer v1.34, manuale (mercato/setup/asta/glossario/footer), driver `test/partita_smart.js` (asta esclude figure, mano estesa a `slot`). *Collaudo:* suite core verde (`test_partita` NG2/NG4, `test_adatta`, `test_solo`, `test_riprova`, `test_ritirata`); `test_riprova` migrato a **seed 5** (O,O,P,P,P), più robusto del vecchio seed 2 rispetto alla non-determinismo del percorso IA asincrono. L'esempio §34/Appendice B resta obsoleto (da rifare con nuovo seed-vetrina).
- **Regola v1.33: il Jolly speculare — cattura sleale (P) e spinta che fa emergere il peccato (O), a zero punti** (luglio 2026): lungo ragionamento di design con Saverio, guidato dal benchmark (`test/benchmark.js`). Il vecchio Jolly (carta nel mazzo dei Protagonisti dopo la 2ª scena, che regalava punti all'avversario) è stato ridisegnato e misurato in più giri. Percorso: (a) Jolly-risorsa che regala punti a O → P crolla dal 65.6% al 51% (i punti a O ribaltano la missione); (b) versione speculare a 0 punti → P ~52.8%, e si scopre che il calo NON veniva dai punti ma dall'**uso stesso** del Jolly, e — verificando `rFinale` — che la missione la decide il **piatto della scena 5 + Crescita**, non le poste (che sono l'arco narrativo). Chiarimento chiave di Saverio: **vincere le scene è ciò che conta narrativamente**, i punti finali sono consolazione; e il gioco ottimale è usare il Jolly **solo per ribaltare una scena che altrimenti perderesti**. **Design finale:** il Jolly è una **risorsa dei Protagonisti fuori dal mazzo**, afferrata seppellendo una carta in fondo al proprio mazzo; **usarlo non dà punti a nessuno**; P lo gioca come cattura elastica (1-10) nelle **scene 1-3** (il peccato), poi **passa all'Opposizione**, che nelle **scene 2-4** lo spende come **spinta** (presa→scopa) facendo **emergere il peccato** — mai due usi nella stessa scena (O solo in una scena successiva a P). Chi **non** lo usa e lo tiene a fine partita incassa **+2 Crescita** (solo nel conteggio finale, non spendibili al mercato). Il costo del peccato non è in punti ma nell'**armare l'avversario** (gli passa il Jolly): il benchmark conferma il tema, usarlo aggressivamente fa vincere MENO scene perché arma O. Esito col gioco ottimale: missione P **53.6%**, poste **2.94-2.06**, arco P avanti / O in scena 4 / finale in bilico; Jolly usato **0.53/partita** (cattura P 0.46, spinta O 0.08). **Propagazione (stesso commit):** motore `index.html` (risorsa `lato.jolly`, `avviaUsaJolly`, `giocaJolly` a 0 punti che passa il Jolly, `jollySpintaDisponibile`/`spintaJolly`, `premioJollyNonUsato`, `jollyPassatoScena`, finestre in `jollyUsabile`; rimossi `entraJollyNeiMazzi`, fase `jolly_intro`/`rJollyIntro`, `jollyMarkerO`, `G.jollyPeccato`), `REGOLE`/`REGOLE_IA`/`statoPerIA`, KB §22 riscritto + §7/§8/flusso/registro/footer v1.33, manuale Capitolo 5 + glossario + setup + footer, driver `partita_smart.js` (usi P/O separati, metrica `jollyP`/`jollyO`) e `benchmark.js`. *Collaudo:* suite core verde (`test_partita` NG2/NG4, `test_adatta`, `test_solo`, `test_riprova`, `test_ritirata`); benchmark v1.33 su 250 seed. *Nota:* direzione futura concordata con Saverio — la Crescita diventerà solo **colore narrativo delle vignette** e la missione resterà decisa dalla scena 5 dopo i colpi; allora il "+2 se non usato" sarà flavor puro. L'esempio §34/Appendice B resta obsoleto (da rifare con nuovo seed-vetrina; l'Appendice B pre-ridisegno è archiviata in `docs/archivio/`). *Gap noto:* la modalità vs Claude non offre ancora a Claude il pulsante della spinta-Jolly di O nel percorso di gioco automatico (`iaTurno`/`iaNarrazione`); la UI umana e il benchmark sono completi.
- **Regola v1.32: punteggio a gettoni, le carte non fanno più da marcatori** (luglio 2026): richiesta di Saverio. Fino alla v1.31 i punti erano già una valuta lineare, ma al tavolo si **tenevano fisicamente** con le carte davanti al giocatore (prese in verticale = 1, scope in orizzontale = 3), e spenderli al mercato voleva dire spostare quelle carte negli scarti. Il problema: una scopa è una carta indivisibile da 3, quindi **dare il resto dopo il mercato** era scomodo. Decisione: i punti diventano un **numero** segnato con gettoni o su un foglio, slegato dalle carte. Conseguenza scelta (con Saverio, tra due modelli): **tutte** le carte giocate — presa/scopa, catture, figure sacrificate — vanno **subito negli scarti comuni** all'atto della giocata; spendere al mercato non muove più carte (si sottrae il costo, resto immediato). Questo **cambia il bacino del Re** (§21): gli scarti ora si riempiono a prescindere dagli acquisti, quindi il Re pesca da un bacino più ricco. Il Jolly: i **punti** vanno all'avversario come numero, la **carta del Jolly** gli è consegnata come promemoria del peccato (la licenza narrativa §22.6 vale finché ha davanti quella carta). **Lavoro fatto in due passi, su scelta di Saverio:** (1) testi: manuale (già editato da Saverio; rifiniti glossario/§4.2/refusi), KB §17/§20/§15.3/§18.1/§22.3/§22.6 + registro v1.32, `index.html` `REGOLE`/`REGOLE_IA`, questo diario; (2) motore `index.html` — modello a punti numerici (`L.punti` al posto degli array `L.prese`/`L.scope` e del `bonus`); carte di prese/scope/sacrifici/resa negli scarti alla giocata; `pagaCosto` ridotta a `L.punti -= costo`; il Jolly assegna i punti all'avversario e deposita la carta in `G.jollyPeccato`, su cui ora legge `jollyMarkerO`; lookup della spinta `boostCard` dagli scarti; rimosso il campo morto `.esposta`. Adeguati i driver di test (`test_partita.js`, `partita_esempio.js`, `partita_smart.js`), che contavano i punti leggendo gli array; in `partita_smart` lo scoring non è più iniettabile via replace (ora è fisso, presa 1/scopa 3). **Collaudo core verde** (`test_partita` NG2/NG4, `test_adatta`, `test_solo`, `test_riprova`). *Resta da fare:* **rigenerazione dell'esempio canonico** (§34/Appendice B) — il bacino degli scarti cambia e con esso l'effetto del Re — da fare insieme alla migrazione a seed 34/367 già pendente da prima (l'errore scriptato del seed 23 in `partita_esempio` preesiste al cambio).
- **Regola v1.30: le otto spinte sono tutte spendibili (due per seme), una per presa** (giugno 2026): nato da una revisione del regolamento fatta fare a una chat senza contesto, confermata da Saverio. Il pitch fa scrivere **otto spinte** (due per seme per lato), ma il regolamento e il motore ne consentivano una sola **per seme** in tutta la partita: di otto scritte, solo quattro erano davvero giocabili al tavolo, e le due seconde spinte di ciascun lato restavano inerti. La frase «una spinta per seme per lato» (KB §9.8, manuale §3.10/glossario/Appendice G, `REGOLE`/`REGOLE_IA`/wizard) **non era testo stantio**: descriveva esattamente il comportamento del motore, che bruciava il seme intero (`G.spinte[l][seme]=true` in `eseguiSpinta`, filtrato in `spinteResidue`/`spinteDisponibili`). Ora **tutte e otto sono spendibili**, ciascuna una sola volta: le due dello stesso seme sono due cariche distinte (varietà di scelta, non potenza accumulabile). Aggiunta la condizione **«una sola spinta per presa»** (già di fatto garantita: dopo la conversione `G.narr.boostCard` si azzera, quindi la presa-ormai-scopa non è più spuntabile). **Code change minimo:** tolta l'assegnazione per-seme e i due filtri `!G.spinte[l][p.seme]`; il vincolo «una volta sola» resta sul flag per-spinta `pitch[i].usata` (già esistente); rimosso lo stato ora morto `spinte:{P:{},O:{}}` dall'init di `G`. **Equilibrio:** non è "raddoppio di potenza" ma sblocco delle cariche già scritte; in pratica un lato può ora trasformare in scopa fino a due prese dello stesso seme in scene diverse. **Propagazione** (stesso commit): KB §9.8 (ora quattro condizioni) + nota v1.2 (tolto «assorbe il limite per seme») + registro v1.30 + footer; manuale §3.10/glossario/Appendice G (tre etichette) + footer (era fermo a v1.26 → v1.30); `index.html` motore + `REGOLE` + `REGOLE_IA` + due pagine «spinte» del wizard. **Collaudo:** suite verde; transcript canonico invariato (le scene 1-4 spuntano spinte di semi diversi, nessuna usa due spinte dello stesso seme). Contestualmente sanato un **residuo della v1.29** che la sua propagazione aveva mancato: la parentesi del manuale §7.3 («la missione, se i totali del piatto sono pari, si decide prima per numero di scope e poi per numero di prese») contraddiceva il tie-break sulla Crescita (§25 KB / manuale §6.3, già corretti); rimossa.
- **Bugfix vs Claude: deadlock su "Claude sta generando" a fine scena** (giugno 2026): segnalazione di Saverio (due partite dal sito bloccate dopo la narrazione dell'ultima carta della scena). Con la regola v1.29 (l'esito di fine scena lo narra chi ha **perso la posta** = `altro(vincitore)`, in parità chi ha vinto l'asta) la decisione "tocca a Claude narrare l'esito" è centralizzata in `latoCheAgisce()`, ed era già allineata in `claudeStaGiocando()` (overlay) e in `rFineScena()` (slot esito di Claude + pulsante disabilitato). Ma `iaScheduler()` lanciava `iaEsitoScena()` ancora sulla **vecchia** condizione `altro(rec.iniziativa)==="O"` (chi ha giocato l'ultima carta del round). Le due divergono quando i Protagonisti **vincono** la scena ma l'Opposizione **aveva l'iniziativa** (o in parità con iniziativa all'Opposizione): l'overlay si mostrava e il "Vai al mercato" restava disabilitato in attesa di Claude, ma la generazione non partiva mai → schermata "Claude sta generando" all'infinito. Corretta la condizione di `iaScheduler` in `!perRitirata && latoCheAgisce()==="O"`, così tutte e tre le rappresentazioni leggono l'unica fonte di verità. Solo `index.html` (modalità solo); nessun effetto su KB/REGOLE (la regola v1.29 era già propagata). Collaudo invariato (test solo/riprova verdi); l'errore del transcript seed 23 preesiste ed è il WIP di migrazione a seed 34/367.

---

## 1. Decisioni di regolamento (confluite nella KB, da v1.1 a v1.6)

Queste sono ormai regole ufficiali, integrate nel regolamento e marcate da *(v1.1)* a *(v1.6)* nel documento:

1. **Mazzi base da 20 carte** (Asso–10 dei due semi per lato). Le carte bastano esattamente per cinque scene: quando il mazzo finisce, la partita finisce. Nessun rimescolo degli scarti nel mazzo. *(La versione da 14 carte, 1–7, era un errore: la simulazione automatica ha dimostrato che i mazzi si esauriscono alla quarta scena.)*
2. **Carta d'asta del perdente**: torna nella sua mano. Quella del vincitore resta da parte e viene giocata come sua quarta e ultima carta del round.
3. **Round = 4 carte a testa**, alternanza completa (8 giocate), nessuna carta resta in mano a fine round.
4. **Parità nell'asta**: l'iniziativa va a chi non l'ha avuta nella scena precedente. Alla prima scena, in caso di parità, va all'**Opposizione** ("il mondo è contro di voi fin dall'inizio"). Non esiste più la ripetizione dell'asta.
5. **Il piatto resta** sul tavolo tra una scena e l'altra: chi apre la scena successiva può già fare presa o scopa.
6. **Spinta del pitch = scopa (§9.8, v1.2)**: si spunta la risposta corrispondente al **seme della carta di presa** e la presa diventa una scopa a tutti gli effetti, le carte residue del piatto vanno tra le prese di chi spunta. **Vietata nell'ultima scena** (nella Risoluzione il piatto si svuota solo con una scopa). Il vincolo di seme assorbe il limite di una spinta per seme per lato; effetto collaterale accettato: una figura comprata di seme avversario non sblocca spinte. Sostituisce la "finta scopa" della v1.1 (carta tra le scope, piatto intatto). Da osservare al playtest: il peso dello svuotamento del piatto nelle scene 3 e 4, dove il valore accumulato dal piatto persistente è massimo.
7. **Colpo di scena**: la carta avversaria sostituita va negli **scarti del suo proprietario** (quindi può teoricamente rientrare con un recupero o un Re).
8. **Maggioranza di seme (3/4 giocatori)**: si valuta **a inizio scena** sulla mano appena pescata e il controllo vale per l'intera scena, asta inclusa. Alla prima scena, in parità, il controllo parte da Picche (Protagonisti) e Quadri (Opposizione).
9. **Prima difficoltà (§9.7, v1.2)**: dopo le due serie di domande del pitch, tutti rispondono insieme a "che cosa vi impedisce di raggiungere l'obiettivo subito?". La risposta **non è spuntabile**: appartiene al tavolo e serve a inquadrare la Scena 1 (Innesco). L'uso meccanico delle risposte è diventato §9.8. Nel gioco digitale: campo in coda al passo "Le domande" del wizard, riepilogo nel passo finale, promemoria nell'apertura della Scena 1.
10. **L'Opposizione prima delle domande (§9.4, v1.2)**: la definizione di nemici ♦ e caos ♣ precede le domande del pitch (nuova sezione 9.4; le domande passano a 9.5–9.6), così le domande all'Opposizione hanno un riferimento concreto. Stesso ordine nel wizard: Il gioco → Giocatori → Tono → Missione → Protagonisti → **Opposizione → Le domande** → Pronti a partire.
11. **Copia unica delle figure (§20, v1.2)**: ogni figura di ciascun seme esiste in una sola copia e può essere acquistata una sola volta in tutta la partita (nel mercato il pulsante sparisce dopo l'acquisto, stato in `G.figureComprate`). Emersa durante la generazione della partita d'esempio, in cui l'Opposizione aveva comprato due Fanti di Quadri.
12. **Scenario demo (§33, v1.2)**: "Frank, Skunk e il jet di Vargas" è il pitch canonico usato in **tutti gli esempi** del regolamento (KB e regolamento in-game) e nella futura partita d'esempio. Frank (♠): ex poliziotto sospeso, terra bruciata attorno. Skunk (♥): suo ex informatore, sa dove atterra Vargas e come superarne la sicurezza grazie all'ultima telefonata d'addio del fratello Jorge (ucciso al posto suo: la spia era Skunk, e Frank non lo sa); non rivela nulla (assicurazione sulla vita + vuole esserci). La telefonata di Jorge è il flashback canonico degli esempi. Vargas non può spostare il meeting (boss in arrivo, finestra di volo del jet). Prima difficoltà: Skunk in cella, messo lì dai poliziotti corrotti per farlo uccidere in prigione.

13. **Scena chiusa da una scopa (§18, v1.3)**: se la scena finisce con il piatto vuoto perché l'ultima giocata è stata una scopa, la posta è vinta da chi ha fatto la scopa (non più parità 0-0). Vale anche per l'esito apparente della Scena 5 (poi seguono i colpi di scena). Tracciato con `G.svuotoConScopa` (impostato in `giocaCarta`, `eseguiSpinta`, scopa col Jolly; azzerato a inizio scena, su giocata nel piatto, su presa e sul counter del Jolly — che svuota ma non è scopa). `chiudiScena` assegna `rec.vincitore=svuotoConScopa` e marca `rec.perScopa`.
14. **Colpi di scena riscritti (§24, v1.3)**: si gioca a turni alterni con la riserva; finché si hanno carte **si gioca obbligatoriamente**. La carta entra nel piatto (somma il suo valore) e può **eliminare** una carta di seme avversario di valore **pari o inferiore** (va negli scarti; la carta giocata resta nel piatto). Niente prese/scope; figure solo a valore. Fine quando entrambe le riserve sono vuote. Sostituisce la vecchia "sostituzione di una avversaria di valore maggiore o uguale". `colpiPossibili(l)` = riserva non vuota; `bersagliColpo`, `prossimoColpo` (alternanza), `eseguiColpo` (carta→piatto, bersaglio→scarti); IA `iaColpi` sceglie carta+bersaglio che tolgono più valore (la narrazione la fa `iaNarrazione`). Driver di test e `test_solo` aggiornati: nessun "passo", colpo obbligatorio. Riserva ora vuole carte **alte** (hint IA della mano estesa aggiornato).
15. **Transcript seed 13 rigenerato (v1.3)**: `test/transcript_seed_13_v2.txt` è il riferimento aggiornato a tutte le regole v1.3; il §34 del regolamento è stato riscritto su questo transcript (vedi punto 18).

16. **Economia a marcatori e scarti comuni (§17/§20, v1.3)**: ogni presa vale 1 punto, ogni scopa 3 (mercato e finale identici; i punti non spesi mantengono il valore). Si tengono solo le carte che fanno presa/scopa come marcatori (`L.prese`, `L.scope`); le carte catturate dal piatto vanno subito in `G.scartiComuni` (pila comune, sostituisce i `lato.scarti` per-lato). `puntiMercato`=`punti` finale=`bonus + prese×1 + scope×3`. `pagaCosto` spende bonus→prese→scope, marcatori spesi → scarti comuni (niente più "tappata"). Recupero dei Protagonisti ed effetto Re pescano da `G.scartiComuni` filtrando per seme; colpi di scena, counter del Jolly e scarto della Regina ci scaricano dentro. Il counter vale 1 presa (tiene 1 carta come marcatore, il resto agli scarti).
17. **Figure: prezzi e una per tipo (§21, v1.3)**: costi Fante 4, Regina 7, Re 10 (valori in gioco 8/9/10 invariati). La figura di mezzo è rinominata **Regina** (`FIGURE.D.nome`). Vincolo: una figura per tipo per lato (`figuraTipoComprata(l,f)`), sostituisce la copia-unica-per-seme; nel mercato, comprato un tipo, spariscono entrambi i pulsanti di quel tipo per quel lato.
18. **§34 riscritto (v1.3)**: la partita d'esempio narrata "Trentasei ore" è stata riscritta sul transcript seed 13 rigenerato, allineando tutto alle regole v1.3 (spinte = scopa, economia a marcatori/scarti comuni, costi figure 4/7/10, una figura per tipo, Jolly che regala il punto ed entra dopo la 2ª scena, colpi di scena obbligatori). Esito d'allora: Vittoria Totale, diario su-su-giù-giù-su. *(Aggiornamento v1.4: con la cattura obbligatoria, vedi punto 22, il seed 13 cambia mosse ed esito — ora "Per il rotto della cuffia", stesso diario su-su-giù-giù-su; §34 riallineato.)*

19. **Jolly ridisegnato (§22, v1.3)**: un solo Jolly, **solo nel mazzo dei Protagonisti**, entra dopo la **2ª scena** (`entraJollyNeiMazzi` su un solo lato; ingresso e `jolly_intro` a `G.scena===1`). Si usa solo per catturare (`opzioniJolly`→`giocaJolly`): il marcatore va nel campo dell'**avversario** (presa→`A.prese` 1 pt, scopa→`A.scope` 3 pt), ma chi gioca conserva il vantaggio di scena (`G.svuotoConScopa=l`). A piatto vuoto col solo Jolly → scartato (`G.scartiComuni`). **Rimossi**: counter (`rCounter`/`risolviJolly`/`iaCounter`/fase `counter`/`pendingJolly`) e peccato latente (`peccatoDisponibile`/`eseguiPeccato`/`peccatoScena`/pulsanti in turno e mercato). L'IA non ha più il ramo Jolly (l'Opposizione non lo possiede mai).
20. **Spinte solo "meccaniche", i due "detesta" diventano tratti (§9.5/9.8, v1.4)**: le due domande "cosa A detesta di B / di A" sono **solo narrative** (tratti dei protagonisti, nessun effetto), spostate come **terzo campo** nella pagina Protagonisti del wizard (passo 4, `#w-pitch-0`/`#w-pitch-1`). Le spinte spuntabili sono **8, due per seme**. `PITCH_TEMPLATE` ha ora `spinta:false` sugli indici 0/1 e un `ph` (placeholder) per voce; `spinteResidue`/`spinteDisponibili` filtrano `PITCH_TEMPLATE[i].spinta!==false`. `G.pitch` resta 10 voci (churn minimo, indici invariati). Form: passo 6 ("Le spinte") mostra solo gli indici 2-9, con la riga "Portando in narrazione questo aspetto potrai trasformare una presa in una scopa" e placeholder. **Formato playset** aggiornato: `pers.A/B.detesta` + `spinte:{picche,cuori,quadri,fiori}` (2 ciascuno); `caricaPlaysetObj` resta tollerante al vecchio `pitch` piatto, `esportaPlaysetFile` produce il nuovo schema; embedded `PLAYSET` e `playsets/frank_skunk_playset.json` riscritti. Esempio §34: la spinta ♠ del seed 13 non è più il "detesta" (indice 0) ma la **motivazione di Frank** (indice 2); transcript rigenerato e beat §34 riscritto. KB §9.5/9.8 e §33 aggiornati.
21. **Significato narrativo lato-dipendente di introdurre/presa/scopa (§14/§15, v1.4)**: chiarito che i tre gesti cambiano senso a seconda del lato. **Introdurre** (giocare nel piatto) = ogni lato dispone una propria risorsa, non risolve nulla: per i Protagonisti un elemento/fatto/azione che prepara un vantaggio futuro, per l'Opposizione un ostacolo o una minaccia. **Presa** = togliere una risorsa all'avversario (la carta tolta dal piatto è l'appiglio narrativo); è un **successo** vero, eventualmente con un costo, **non** un "superamento parziale" (Protagonisti: superano un ostacolo; Opposizione: tolgono o rendono inutile un vantaggio dei protagonisti). **Scopa** = successo pieno **sopra ogni aspettativa**. In `index.html`: `suggerimento(c,tipo,dettagli,latoNarr,stessoLato)` ora ramifica P/O (il lato si passa da `vaiNarrazione` come `narrLato||G.attore`, perché `G.narrLato` è impostato *dopo* la chiamata); allineati banner di `giocaCarta`, `descrizioneMossaO`, `REGOLE` e `REGOLE_IA`. KB: §14, §15.1/2/3, tabella §riepilogo, registro modifiche. Esempi riscritti per entrambi i lati.
22. **Presa obbligatoria e azione maldestra (§16, v1.4)**: se la carta giocata può fare una presa o una scopa, **non** può essere messa nel piatto: la cattura è obbligatoria. Obbligo **per-carta** (non per-turno: puoi mettere nel piatto un'altra carta che non cattura) e **scelta libera** tra le catture possibili (nessuna priorità forzata singola vs gruppo, presa vs scopa). In `index.html`: `mostraOpzioni` non emette il bottone "Metti nel piatto" se `combosPresa(c.val).length>0`; `giocaCarta` ha una guardia difensiva (rifiuta `combo=null` con catture disponibili, escluso il Jolly); `iaTurno` non offre più l'opzione "nel piatto" quando la carta cattura. Quando si è costretti a prendere una carta del **proprio lato** (flag `stessoLato` calcolato in `giocaCarta` su `semiLato(l)`), si narra un'azione maldestra in cui le due anime del lato (Cuori/Picche o Fiori/Quadri) si sono ostacolate o annullate. Transcript seed 13 rigenerato (cambiano i banner e la scelta delle mosse) e §34 riallineato.
23. **Il peccato emerge: terzo uso del Jolly per l'Opposizione (§22.6, v1.5)**: il marcatore del Jolly è la carta Jolly stessa, che dopo la giocata sta in `O.scope`/`O.prese` (la trova `jollyMarkerO()`): nessun flag nuovo, il "consumato" è naturale (sparisce quando spesa al mercato o usata come effetto). Predicato `peccatoEmergeDisponibile()` = fase `turno`, attore `O`, marcatore presente e almeno una carta dei Protagonisti (♥/♠) nel piatto. Handler `eseguiJollyEffetto(testoNarr)`: consuma il marcatore (→ `G.scartiComuni`), toglie dal piatto la carta di valore più basso dei Protagonisti (→ `G.scartiComuni`), `G.svuotoConScopa=null`, banner in `G.ultimaGiocata`, voce di log `aggiungiStoria` con `tipo:"peccato"` (icona 🃏 in `mossaInfo`), poi `render()` (propaga in online via `inviaStato`, rischedula `iaScheduler` in solo). UI: bottone `#btnPeccato` in `rTurno`, mostrato solo se `peccatoEmergeDisponibile()` e non in solo. Solo: in `iaTurno`, prima della giocata, se disponibile Claude decide (`{usa,narrazione}`) e in caso affermativo chiama `eseguiJollyEffetto`; senza chiave/fallback non lo usa, quindi la partita non si blocca. Non durante i colpi di scena (la fase `colpi` non passa da `rTurno`/`iaTurno`). Operazioni per id, nessuna mutazione nei render. Collaudo: `test_partita` 2/4 e `test_solo` verdi; aggiunto `test/test_peccato.js` (rimozione della carta più bassa, consumo del marcatore, indisponibilità senza bersaglio).
24. **Pacchetto v1.6: punteggio 1/2/4, costi 3/5/8, cattura flessibile delle figure, sacrificio, resa**:
    - **Punteggio (§17/§20)**: presa con carta numerica 1 punto, presa con **figura** 2, scopa 4 (mercato e finale identici). `puntiMercato`/`punti` finale = `bonus + Σprese(fig?2:1) + scope×4`. `pagaCosto` spende bonus → prese numeriche → prese con figura → scope.
    - **Cattura flessibile delle figure (§16)**: una figura cattura un gruppo di somma **minore o uguale** al suo valore (Fante 8, Regina 9, Re 10), anche carte dei propri semi; obbligatoria se esiste un bersaglio alla portata. `comboFiguraDefault(l,val)` precompila la selezione; `opzioniFigura(c,daAsta)` mostra il piatto selezionabile con `#figCattura` (1≤somma≤val), `#figMetti` se nessun bersaglio, `#figSacrificio` se piatto vuoto. `giocaCarta` accetta sottoinsiemi ≤ val (non valida la somma esatta). Se svuota il piatto è scopa (4), altrimenti presa con figura (2); l'effetto speciale scatta comunque.
    - **Sacrificio (§15.3)**: figura su piatto vuoto → scopa con sé stessa (`combo==="sacrificio"`, va in `L.scope`), imposta `G.svuotoConScopa`, l'effetto scatta lo stesso. Asimmetria col Jolly, che su piatto vuoto si scarta.
    - **Costi figure (§21)**: Fante 3, Regina 5, Re 8 (valori 8/9/10 invariati); due strade pari a 8 (un Re, oppure un Fante più una Regina).
    - **Resa (§18.1)**: chi non ha l'iniziativa (`G.attore!==G.astaWinner`), con una sola carta (`mano.length===1`), nelle scene 1-4 (`G.scena<4`), può cedere la scena. Due varianti, una volta ciascuna per lato (`G.ritirata[l]={mazzo,prese}`): **Ritirata strategica** (rimescola la carta nel mazzo, +1 riserva per i colpi) e **Resa onorevole** (carta tra le prese, 1 o 2 punti). `ritirataDisponibile`/`ritirataVarianti`/`eseguiRitirata`/`chiediRitirata` (modale narrazione + `#ritConferma`); due bottoni `#btnRitirata`/`#btnResa` in `rTurno`. `chiudiScena` con `G.ritirataScena` assegna la posta all'altro lato (`rec.perRitirata`, `rec.ritirataVariante`); se cede l'Opposizione i Protagonisti recuperano. La carta non entra nel piatto, che resta com'è e passa alla scena dopo. In solo l'esito lo narra Claude; il prompt di `iaTurno` riceve i totali del tavolo e il consiglio (arrendersi solo se la carta non fa presa e non porta sopra i Protagonisti). Schede: `resaTracker(l)` con i due checkbox.
    - Allineati `REGOLE`, `REGOLE_IA` e KB (§15.3, §16, §17, §18.1, §20, §21, §22.3/§22.6; footer v1.6, registro). `test/test_ritirata.js` aggiornato al design a due bottoni + `#ritConferma` (prima cliccava `ritMazzo`/`ritPrese`, id mai esistiti in questo design: non era una regressione del motore). Conferma di taratura: 960 partite simulate (480 a 2g + 480 a 4g) col motore completo confermano 3/5/8 (Re abbordabile circa 59% a fine scena 3, circa 83% a fine scena 4); resa circa 1.3/partita, più frequente lato Opposizione, crescita riserva circa +1 carta/partita.
    - **Regina pesca dalla riserva nell'ultima scena (§21)**: l'effetto della Regina fa scartare all'avversario una carta a caso e la rimpiazza con la prima del mazzo; se nell'ultima scena il mazzo è esaurito ma c'è una **riserva**, il rimpiazzo si pesca dalla riserva (`A.mazzo.length>0 ? A.mazzo.shift() : A.riserva.length>0 ? A.riserva.shift() : nessuna`), così l'avversario non resta a corto di una carta finché ha carte messe da parte per i colpi. Vale solo per la Regina (il Re pesca dagli scarti, il Fante guarda solo la cima senza prendere). Allineati KB §21 e §23.1, registro v1.6. Conseguenza di design da osservare al playtest: la Regina è più debole nell'ultima scena, perché converte una carta di riserva in carta di mano invece di togliere una carta netta all'avversario.
    - **Parità ai punti nell'outcome (§26)**: il pareggio va a chi ha vinto il piatto. Motore: `piattoP ? (puntiP||pari ? Vittoria totale : per il rotto della cuffia) : (puntiP ? Sconfitta con aspetti positivi : Disfatta totale)`. Quindi piatto vinto + pari = Vittoria totale, piatto perso + pari = Disfatta totale. Documentato in §26 con una nota; il tie-break del piatto resta scope, poi prese.
    - **§34 rigenerata con la regola della Regina**: il seed 13 cambia esito nell'ultima scena. Omar gioca la Regina, Paola scarta il 3♥ e pesca il 7♥ dalla riserva, col quale fa presa sul 7♠; primo conteggio 4-0 Opposizione, colpi aperti dai Protagonisti (6 colpi); piatto finale 22-7, punti 5-5 (pari), esito **Vittoria totale** (prima era "per il rotto della cuffia", 31-0 / 4-5). Diario invariato: su, su, giù, giù, giù. Transcript `test/transcript_seed_13_v2.txt` riscritto; scene 1-4 invariate.
25. **Guida narrativa alla posta e all'introdurre (§6, §12.2, §12.3, §15.1, v1.7)**: tre chiarimenti narrativi (nessun effetto sul motore, transcript seed 13 invariato).
    - **Come scrivere la posta (§12.2)**: la posta è una **difficoltà concreta** o un **obiettivo intermedio** (procurarsi qualcosa, entrare o uscire da un luogo) e va formulata esplicitando **entrambi i rami** (cosa accade se la vincono i Protagonisti, cosa se vince l'Opposizione). Perderla non pregiudica mai del tutto l'obiettivo finale, che resta raggiungibile **fino all'ultima scena**.
    - **Continuità tra le scene (§12.3, nuova sezione)**: ogni scena si apre dall'esito della posta precedente; lo stato dei protagonisti è conseguenza diretta di com'è finita la scena prima (se non sono usciti dalla cella, la scena dopo non può iniziare con loro in fuga per strada).
    - **Introdurre non annulla (§15.1)**: la narrazione di una giocata nel piatto aggiunge un proprio elemento e non può cancellare un elemento avversario già in gioco; un elemento dell'altro lato si neutralizza solo con una presa o una scopa (il fatto narrativo segue il fatto meccanico).
    - Allineati `REGOLE` (sezioni "Asta e apertura" e "Azioni e prese", con esempio `.es`) e `REGOLE_IA` (blocchi ASTA E APERTURA e ROUND/INTRODURRE) in `index.html`, più §6 e changelog v1.7 del KB.
    - **I `suggerimento_posta` dei 5 scenari (index.html) riscritti in forma esplicita** "obiettivo. Se va bene: ... Se va male: ...", per applicare il concetto anche ai semi pre-compilati mostrati nella striscia scene. Lo scenario demo (Frank/Skunk) è anche la fonte della partita d'esempio: aggiornato di conseguenza l'array `POSTE` in `test/partita_esempio.js`, rigenerato `test/transcript_seed_13_v2.txt` (cambiano solo le 5 righe "Posta", il gioco meccanico è identico) e riallineate le 5 righe `*Posta:*` del §34 nel KB. In un secondo momento la forma bilaterale è stata propagata a **tutti i file dei playset** (JSON e schede di frank_skunk, camion giallo, costa delle ginestre, coupé di papà, sole di mezzanotte), così le tre rappresentazioni di ogni playset (embedded in index.html, JSON, scheda) coincidono.
26. **I nemici (♦) possono essere attori diversi (§9.4, v1.8)**: il seme Quadri non deve per forza essere un'unica fazione coesa. Possono essere attori diversi, perfino rivali tra loro, che per ragioni differenti vogliono la stessa posta o ostacolano i protagonisti; li accomuna solo il remare contro, e si esprimono tutti sul seme ♦. Chiarimento solo narrativo (nessun effetto sul motore). Esempi già nei playset: "Sole di Mezzanotte" (Loggia vs Veglianti, opposti ma entrambi ostili) e "camion giallo" (Mirko collezionista + Cloe gelosa, moventi diversi). Propagato a KB §9.4 e registro v1.8, `REGOLE` (sezione "I quattro semi"), `REGOLE_IA` (descrittore ♦ nell'intro) e label/placeholder del passo "Opposizione" nel wizard (tolta anche una lineetta lunga dal label).

27. **Chi narra l'esito della scena (§13, §18, v1.9)**: chi vince l'asta inquadra la scena (titolo e posta), ma a narrare com'è finita è chi ha **perso l'asta**, cioè chi gioca l'ultimissima carta del round (o chi si arrende), **a prescindere da chi vince la scena**. Nasce da una verifica: il codice affidava la narrazione dell'esito al **vincitore** della scena (in solo, Claude quando l'Opposizione vinceva), non a chi gioca l'ultima carta. Reso coerente. Modifiche al motore/UI in `index.html`: `latoCheAgisce("fine_scena")` ora ritorna `altro(iniziativa)` (instrada la fase di esito al perdente dell'asta anche online); `claudeStaGiocando`, `claudeEsito` (in `rFineScena`), `conteggioDefinitivo` (scena 5) e `iaScheduler` passano da `vincitore==="O"` a `altro(iniziativa)==="O"`; `istruzEsito` riscritta per narrare anche scene vinte dai Protagonisti (più `esitoFallback` condizionale per i due fallback). Propagato a `REGOLE` (sezioni "Asta e apertura" e "Fine scena e letture", nuovo blocco "Chi narra l'esito"), `REGOLE_IA` (blocco "FINE SCENA"), KB §13/§18 e registro v1.9, manuale del giocatore. Aggiornato `test/test_riprova.js` (la replica di `claudeEsito` usa ora `iniziativa==="P"`). Nessun effetto sul motore: transcript seed 13 invariato (a meno del newline finale); `test_partita` 2/4, `test_adatta`, `test_solo`, `test_riprova` verdi.

28. **Due principi per una buona posta: Climax e Fail forward (§12.2, v1.10)**: guida narrativa su come inventare la posta. **Climax**: la posta cresce di scena in scena (sempre più drammatica, ardua, decisiva), senza bruciare subito le poste estreme, o un fallimento precoce dirotta la storia (es. un'evasione) lontano dalla missione; si appoggia alla curva delle 5 scene. Inclusa una scala di esempio non vincolante: Scena 1 tempo, Scena 2 una risorsa preziosa, Scena 3 motivazione/morale, Scena 4 tenuta dell'alleanza, Scena 5 esito dell'intera avventura. **Fail forward**: anche persa, la posta lascia ai protagonisti esperienza, un'opportunità o una risorsa per proseguire verso l'obiettivo, senza bloccarli in un vicolo cieco frustrante. Solo guida narrativa, nessun effetto sul motore (transcript seed 13 invariato). Propagato a KB §12.2 e registro v1.10, `REGOLE` ("Come scrivere la posta") e `REGOLE_IA` ("ASTA E APERTURA") in `index.html`, e manuale del giocatore (nuova sezione "Come inventare una buona posta" in §5.2).

29. **Rimosso il recupero dei Protagonisti a scena vinta (§18, v1.11)**: vincere la posta non fa più recuperare una carta del proprio seme dagli scarti comuni. In `chiudiScena` rimosso il blocco (filtro semi P + pesca casuale + push in mazzo); `rec.recupero` resta a `null` (il banner di `rFineScena` mostra solo "La posta è raggiunta"). Motivazione (dal designer): i Protagonisti hanno già il Jolly come carta in più dopo la 2ª scena, ed entrambi i lati hanno la Ritirata strategica che rimescola una carta nel mazzo, quindi il rientro di carte non è più un'esclusiva. Propagato a KB §8 (ingressi di carte: figure, carta della Ritirata, Jolly), §18, §18.1, §29 e registro v1.11; `REGOLE`, `REGOLE_IA` in `index.html`; manuale §2.1, §5.11, §5.12, flusso §10.3 e diagramma della scena (card "Protagonisti": "si avvicinano all'obiettivo"). **In sospeso:** il motore è cambiato, quindi il transcript seed 13 (`test/transcript_seed_13_v2.txt`) e la partita d'esempio §34/Appendice B vanno rigenerati e riscritti (104/147 righe del transcript cambiano). Da decidere col designer se tenere seed 13 o cercare un seed più illustrativo.

30. **Scenario demo convertito in "Il sole di mezzanotte" + nuovo seed d'esempio (§33/§34, v1.12)**: lo scenario demo passa da Frank/Skunk (poliziesco) a **Vera Falco (♠) e Otto Lenzi (♥)**, caccia archeologica al Sole di Mezzanotte nella cripta di Zerzura, con Loggia del Basilisco + Veglianti come nemici (♦) e il deserto come caos (♣). Motivo: renderlo family-friendly (niente droga/omicidi). Risolve anche l'"in sospeso" del punto 29: invece di rigenerare il seed 13 si è scelto **seed 23**, più illustrativo (mostra tutte le meccaniche senza l'anomalia della doppia-scopa del seed 19). `test/partita_esempio.js` riscritto sullo scenario Vera/Otto, `test/transcript_seed_23_v2.txt` rigenerato (rimossi i vecchi seed 13/19), `CLAUDE.md` portato a SEED=23. **KB §33** riscritto e **KB §34** ("cinque giorni per l'eclissi") narrato scena per scena sul seed 23: piatto finale 30-0 ai Protagonisti ma punti 5-10 (le due scope 6♠/9♠ spese al mercato per Fante♥ e Regina♥, 4 punti regalati a Omar col Jolly), esito **Vittoria per il rotto della cuffia**, diario su, pari, pari, giù, su. Posta della scena 2 riscritta in forma di obiettivo concreto: "Raggiungere El Qara in tempo. Se va bene: si uniscono alla carovana dei mercanti di datteri e ingaggiano le guide indigene. Se va male: arrivano troppo tardi e restano senza quell'aiuto prezioso", propagata a `partita_esempio.js` (array `POSTE`), transcript (solo riga 29, gioco meccanico identico), `playsets/playset_sole_di_mezzanotte.json`, la sua scheda, l'embedded `PLAYSET` in `index.html` e la riga `*Posta:*` del §34. **In sospeso (v1.12):** Appendici A/B del manuale, sweep degli esempi inline ancora Frank/Skunk (KB §27.3 e altri, `REGOLE`/`REGOLE_IA` in `index.html`, esempi del manuale), registro KB a v1.12 + footer, rigenerazione del PDF.

31. **Comprare una figura crea un PNG (§21.1, v1.12)**: all'acquisto di una figura le si dà sempre un nome e una breve descrizione e la si registra su una index card dal lato del proprietario; diventa un PNG a tutti gli effetti e una figura senza nome non si compra. Era già il comportamento dell'app (modale Nome/Descrizione in `chiediPng()` all'acquisto; l'IA in solo usa come default il titolo della figura) ma non era scritto come regola: ora lo è. Propagato a KB §21.1 + registro v1.12, `REGOLE` (sezione "Le figure", nuovo blocco "Comprare una figura crea un PNG") e `REGOLE_IA` (riga nel MERCATO) in `index.html`, manuale §6.3. Emerso scrivendo la fiction del §34 (Saverio: "regola molto importante, va nel capitolo mercato/figure").

32. **Fante consegnato in mano all'acquisto + nuovo seed d'esempio 29 (§21, v1.12)**: il Fante appena comprato non entra più nel mazzo come Regina e Re, ma **entra subito in mano** per la scena successiva (una carta della mano va in fondo al mazzo). Motivo (dal designer): un Fante pescato tardi, o uscito nell'ultima scena, sprecava il suo effetto; così è subito utile. L'effetto del Fante (guarda le prime 4 del mazzo) resta invariato e scatta quando la carta viene giocata. Al mercato prima dell'ultima scena (mano non ancora scelta, mano estesa §23.1) il Fante entra nel mazzo ma resta selezionabile. Motore: ramo Fante in `acquistaFigura` + modale `fanteInMano` in `index.html`, con **auto-risoluzione per l'IA** in solo (manda in fondo la numerica più bassa, tiene Jolly e figure); i driver `partita_esempio.js`/`test_partita.js` gestiscono il modale. Propagato a KB §19/§21 + registro v1.12, `REGOLE`, `REGOLE_IA`, manuale §6.3. **Conseguenza sull'esempio (decisione finale):** la regola cambia le mani dalla 3ª scena. Per non perdere la fiction già scritta delle scene 1-2 (seed 23, salvata in `docs/fiction_esempio_scene1-2.md`) si è adottato un esempio a **DUE SEED**: SEED=23 governa le scene 1-2, SEED2=43 le scene 3-5. Il driver `partita_esempio.js` espone `window.__reseed` e a fine 2ª scena (handler `fine_scena`) rigenera l'RNG con SEED2: rimescola solo il **mazzo residuo** (le carte giocate nelle scene 1-2 sono già fuori dal mazzo), così le scene 1-2 restano identiche e le 3-5 si possono curare. Politica di mercato curata per una "vetrina": Paola compra il Fante (in mano) poi la Regina; Omar punta al Re. Risultato `SEED=23 SEED2=43`: i Protagonisti vincono tutte le poste ma **stravincono il piatto 12-0 perdendo ai punti 2-8** → Vittoria per il rotto della cuffia, diario su-pari-pari-su-giù; in scena compaiono tutte e tre le figure (Fante e Regina di Cuori, Re di Quadri), il Jolly, due spinte (♥/♠) e quattro colpi di scena (con la Regina schierata dalla riserva, che elimina ed è eliminata). Aggiornati `CLAUDE.md` (comando a due seed) e il file di riferimento `test/transcript_seed_23_43_v2.txt` (rimosso il seed 29). §34 in riscrittura narrativa estesa su questa partita; le scene 1-2 della fiction restano valide identiche.

33. **La posta dice il cosa, non il come, e non è mai indispensabile (§12.2, v1.13)**: due principi nella guida sulla posta. **Il cosa, non il come** (nuovo): la posta indica l'obiettivo concreto in palio ma mai il modo di ottenerlo, che deve emergere dalle giocate; va lasciata abbastanza aperta ("orientarsi nel deserto" sì; "orientarsi col taccuino" no, perché prescrive il mezzo e prenarra ciò che dovrebbe nascere dalla scena). **Mai indispensabile** (rafforzato, era già accennato): vincere la posta è un vantaggio, perderla un problema, ma l'obiettivo finale resta raggiungibile fino all'ultima scena in entrambi i casi. Solo guida narrativa, nessun effetto sul motore. Propagato a KB §12.2 + registro/footer v1.13, `REGOLE` ("Come scrivere la posta") e `REGOLE_IA` ("ASTA E APERTURA") in `index.html`, manuale §5.2. Conseguenza sull'esempio: la **posta della Scena 3** passa da "attraversare le dune col taccuino" (prescriveva il mezzo e, se persa, sembrava precludere la meta) a "orientarsi nel deserto e raggiungere l'oasi più vicina a dove dovrebbe trovarsi la città perduta; se va bene si accampano e si preparano alla ricerca, se va male si perdono nel deserto", aggiornata in `playsets/playset_sole_di_mezzanotte.json`, scheda, `index.html`, `test/partita_esempio.js` e transcript `_v2` (cambia solo la riga della posta; gioco meccanico identico). Emerso scrivendo la fiction della Scena 3 con Saverio.

34. **L'esito della scena lo narra chi perde la posta (§13/§18, v1.14)**: capovolge la regola v1.9 (narrava chi perdeva l'asta). Ora racconta com'è finita la scena il lato che ha **perso la posta**, così da poter calcare sulle ombre e sui costi della propria sconfitta (idea di Saverio per dare più "pepe" agli esiti). In caso di **parità** dei totali narra chi ha **vinto l'asta**: serve a non far incatenare due narrazioni di fila a chi ha appena giocato l'ultima carta del round (che è sempre il perdente d'asta). L'inquadratura iniziale resta a chi vince l'asta; la resa (§18.1) è già coerente (chi cede la posta narra). Propagato a KB §13/§18 + registro/footer v1.14, `REGOLE` e `REGOLE_IA` in `index.html`, manuale §5.10. **Motore (3 punti):** `latoCheAgisce('fine_scena')` (riga ~904), `claudeEsito` in `rFineScena` e l'instradamento dell'esito finale in `conteggioDefinitivo` ora calcolano il narratore come `altro(vincitore)` (in parità `iniziativa`). **Test:** `test_riprova.js` default SEED 7→2 (con 7 i Protagonisti non vincevano mai una scena, quindi Claude non narrava mai un esito e la rigenerazione non era testabile; con 2 i vincitori sono O,O,O,P,P). **Actual play:** gli esiti delle Scene 1‑3 (tutte vinte dai Protagonisti, vincitore P) li narra l'Opposizione = Omar; la Scena 1 era già Omar, le Scene 2‑3 passano da Paola a Omar e vanno riscritte nella sua voce.

35. **Euristiche di gioco per l'IA in modalità VS Claude (solo `REGOLE_IA`/`statoPerIA`, non è una regola)**: dettate da Saverio per far giocare meglio l'Opposizione (è Claude a scegliere la mossa, `iaTurno` gli passa le OPZIONI e risponde con `{scelta}`). Quattro euristiche in ordine di priorità, scritte nella sezione STRATEGIA di `REGOLE_IA`: (1) se c'è una scopa, falla (4 punti, svuota il piatto); (2) altrimenti prendi, scegliendo di catturare i semi avversari ♥♠ (alti) ed evitando i propri ♦♣ (presa maldestra); (3) se devi solo calare, porta la **somma del piatto sopra 10** così nessuna carta avversaria (max 10) lo svuota in un colpo (niente scopa subita); (4) se il rischio è inevitabile (piatto vuoto/basso), ragiona sulle **carte già uscite** per capire se le carte capaci di svuotare il piatto sono già state giocate. Per renderle attuabili, due arricchimenti del contesto dinamico in `statoPerIA`: aggiunta la riga **CARTE GIÀ USCITE** (scarti comuni + marcatori di prese/scope dei due lati) e la **somma dei valori nel piatto**; inoltre le opzioni di "cala" in `iaTurno` ora mostrano a quanto **salirebbe il piatto** (`il piatto salirebbe a N`). Nessun effetto su regole, motore o esempio canonico: cambia solo cosa Claude vede e come è guidato (transcript `seed 23/43/5` invariato; `test_partita` 2/4, `test_adatta`, `test_riprova` verdi). Il driver dell'actual play (`partita_esempio.js`) non usa Claude e non è toccato.

36. **Nuovo driver "intelligente" `test/partita_smart.js` (le euristiche al tavolo)**: driver separato che gioca col motore reale applicando le euristiche di #35 per ENTRAMBI i lati, più la regola d'asta dettata da Saverio: vuoi l'iniziativa solo se hai in mano una carta che **scopa subito il carryover** (giochi per primo e la prendi), col **piatto vuoto** la eviti (punto basso) perché aprire nel vuoto espone la carta e rischia di regalare scopa; altrimenti punti basso per non sprecare carte alte. Scelta della mossa per punteggio: scopa (≫) > presa che cattura semi avversari ed evita i propri (penalizza la presa maldestra) > cala portando la somma del piatto sopra 10 > cala col rischio minore secondo le **carte già uscite** (conteggio a informazione equa: solo carte viste, mai la mano avversaria). Il Jolly viene scaricato su piatto vuoto e usato a catturare solo se costretto (regala punti all'avversario); la spinta si usa solo se ripulisce un piatto con residuo ≥4; al mercato ogni lato compra la figura più potente che può permettersi. Modalità `BATCH=N` per esplorare i seed 1..N (un riassunto per riga: scene vinte, scope, punti, esito) usabile come "generatore di seed". **`test/partita_esempio.js` (actual play del manuale) resta INTATTO**: la partita canonica `seed 23/43/5` è invariata (diff verde), così la fiction dell'Appendice B non si tocca. Limite noto e accettato: la difesa "somma > 10" non protegge dalla spinta avversaria (che trasforma qualunque presa in scopa) né dal singolo che cattura una carta appena calata quando quel valore è ancora ignoto, casi inevitabili a informazione nascosta.

37. **Revisione di regole v1.15 (punteggio, Fante, ordine del mercato)**: cinque cambi decisi col designer dopo le simulazioni di bilanciamento sul driver `test/partita_smart.js`. (a) **Scopa = 3** (era 4). (b) **Presa = 1 sempre**, anche con figura (eliminato il bonus a 2). (c) **Fante nel mazzo** come Regina e Re (rimossa l'eccezione "in mano" della v1.12; rimossa `fanteInMano`). (d) **Nuovo effetto Fante**: sbircia le prime 2 del mazzo e, se vuoi, ne scambi una con una carta della mano (la ceduta torna in cima con la sbirciata non scelta; mano e mazzo invariati di dimensione; `effettoFigura` ramo F riscritto, con auto-risoluzione IA in solo; modale `#fanteSwap`/`#fanteNo`). (e) **Ordine mercato → mescola(se hai comprato) → pesca** (spostati pescata e rimescolo da `avviaMercato` a dopo il mercato: `avanzaScena` per le scene 2-4, `avviaManoEstesa` per la 5, nuova `rimescolaSeComprato`). Motivazioni (dal designer + dati): semplificare lo scoring, far sì che si comprino ~2 figure (la 3ª rara), riequilibrare verso il 50/50 (Protagonisti dal ~65% al ~58-60% al gioco ottimale), e rendere il Fante utile presto facendolo capitare già nella scena dopo l'acquisto. Motore in `index.html`: `puntiMercato`/`pagaCosto`, `effettoFigura`, `acquistaFigura`, `avviaMercato`/`avanzaScena`/`avviaManoEstesa`, `iaScheduler`, e tutti i testi `REGOLE`/`REGOLE_IA`/UI. KB: §16/§17/§19/§20/§21/§22, resa §18.1, registro v1.15, footer a v1.15, nota di "stale" sul §34. Driver di test aggiornati al nuovo modale Fante (`fanteNo`); collaudo completo verde (`test_partita` 2/4, `test_adatta`, `test_solo`, `test_ritirata`, `test_peccato`, `test_riprova`). Driver smart aggiornato (default scopa=3/presa=1, nuovo Fante). **In sospeso:** manuale del giocatore (riallineo dei testi e Appendice B), **rigenerazione dell'esempio canonico** (nuovo seed + fiction §34/Appendice B, da fare col designer), `partita_esempio.js` e vecchio transcript curato temporaneamente non validi, rigenerazione PDF.
38. **Definizione del gioco riformulata (§1)**: la definizione d'apertura ora guida con la natura di story-game e il motore competitivo: "uno story-game in cui i giocatori creano insieme una storia, giocando una partita a carte ispirata alla scopa che li vede in competizione su due fronti: Protagonisti e Opposizione. Il voler vincere ogni scena genera la tensione narrativa, creando di continuo nuove difficoltà tra i Protagonisti e la loro missione, il cui esito resta in bilico fino all'ultima carta. È un gioco insieme collaborativo e competitivo, e questa apparente contraddizione è proprio il cuore di Frenemies on the Road". Scelta del designer per mettere in evidenza, fin dalla prima riga, che la sfida tra i giocatori è ciò che produce la storia e tiene l'esito incerto, e che la coesistenza di collaborazione e competizione è il cuore del gioco. Resa valida per 2-4 parlando di "i giocatori ... su due fronti" (non "due giocatori"): i fronti restano due, in 3-4 cambia solo chi tiene le carte (App. C). Nessun effetto sul motore. Propagato a KB §1, manuale §1.1 e intro in-app (`index.html`, schermata di benvenuto `p===0`).

39. **Terminologia «i Qualcuno» (v1.16)**: si battezza un concetto già presente in forma sparsa (il PNG da annotare quando importante; la figura battezzata all'acquisto) e lo si generalizza. Un **Qualcuno** è qualsiasi elemento rilevante della storia (persone, gruppi, popoli, luoghi, oggetti) dotato di **una propria volontà** e di un ruolo o destino che interessa seguire; quando lo si decide gli si dà un nome e lo si annota su index card, e **un Qualcuno senza nome non esiste**. La **volontà** è il discrimine: esclude la scenografia passiva (un portale inerte no, l'Unico Anello sì) e include i collettivi animati da una volontà (la tribù degli Udiburi, la fauna di Pandora guidata da Eywa). **Catturare un Qualcuno collettivo ne neutralizza la presenza nella scena, non l'esistenza nella storia.** La **figura** (Fante/Regina/Re) resta un caso particolare di Qualcuno, quello potente comprato al mercato. Scelta del designer (Saverio, giu 2026) per dare un lessico unico al gesto, già diffuso ma senza nome, di promuovere un elemento a entità ricorrente della storia. **Non** si rinominano gli elementi "PNG" già presenti nel motore/UI (registro PNG, pulsante "Crea PNG"): il termine si aggiunge nei testi di regole, non sostituisce la meccanica esistente. Nessun effetto sul motore. Propagato a KB §15.1 (+ rinvio da §21.1) e registro v1.16, manuale §5.6 (sottosezione "Gli elementi con un nome: i Qualcuno", con le tre mosse ora numerate), `REGOLE` (sezione "Azioni e prese", nuovo blocco "I Qualcuno") e `REGOLE_IA` in `index.html`. Nello stesso passaggio corretta una svista di propagazione v1.15: in `REGOLE` la presa con figura diceva ancora "vale 2 punti" (ora "1 punto come ogni altra", coerente con scopa=3/presa=1 e con la sezione Economia).

40. **Finale a due piani, domande di fine partita ed epilogo a vignette (v1.16)**: revisione della parte finale dettata da Saverio (giu 2026). (a) **Chiarezza sui colpi di scena (cap. 8 / §24)**: nell'ultima scena il **primo conteggio** (dopo le 4 carte a testa) determina e **narra l'esito apparente esattamente come ogni altra scena** (lo racconta chi ha perso la posta); solo dopo, rivelando i **colpi di scena una carta alla volta**, e al conteggio definitivo, si sa se gli eroi hanno compiuto o fallito la missione. (b) **Finale su due piani di lettura (§26)**: primo piano = la **missione** (chi vince il piatto, esito esterno); secondo piano = il **prestigio** (i punti accumulati e non spesi: quanto te la sei cavata con le tue forze; spendere punti in figure = aver chiamato aiuto e diviso il merito, tenerli = essersi fatti un nome). Il prestigio vale per **entrambi i lati**: per i Protagonisti è promessa di crescita, per l'Opposizione minaccia (l'**idra**: per ogni testa mozzata ne ricrescono altre). I quattro esiti sono **rinominati** in **Vittoria piena / Per il rotto della cuffia / Sconfitta dignitosa / Fallimento totale** (Saverio ha cambiato idea rispetto al "tenere vittoria totale": i nuovi nomi vanno ovunque, motore incluso). Un **Fallimento totale dei Protagonisti = Vittoria piena dell'Opposizione**, da narrare come trionfo dell'avversario, non come casella vuota. (c) **Domande di fine partita (§26.1)**: oltre ai due piani, il finale ha **altre due dimensioni** che non entrano nella tabella ma danno voce all'epilogo, il **rapporto** tra i protagonisti (§27.2) e il **privilegio di chi ha vinto più scene**; ciascun giocatore risponde alle domande che lo riguardano: chi ha vinto la missione (hanno ottenuto ciò che volevano?), chi ha vinto il punteggio (come sono cresciuti di prestigio?), chi giocava i Protagonisti (cosa pensa ora Picche di Cuori e viceversa, come si è evoluto il rapporto?), e chi ha **perso la missione ma vinto più scene** (sceglie tra "quale ombra lascio sulla vittoria altrui / quale prezzo nascosto pagherà l'avversario" e "quale riscatto o consolazione traggo"). (d) **Epilogo a vignette (§26.2)**: facoltativo ("cioccolatino"), ciascuna vignetta su un **Qualcuno** (protagonista/PNG/antagonista) ambientata giorni o anni dopo; si narra a turno, **mai due di fila**; "passo" chiude (l'altro narra un'ultima vignetta, poi finisce); **nessun tetto** al numero, **nessun legame** con punti/risorse spese; sapori liberi (chiusura dolce/amara, colpo di scena retrospettivo, gancio per un seguito); è l'**unico spazio in cui un destino dato per certo può essere riaperto** (qui salta la regola d'oro delle scene "introdurre non annulla"). **Scope decise:** epilogo **solo a livello di regole** (in-app resta il campo di testo libero attuale, con la regola "ultima parola a chi ha più scope" tra i due protagonisti); del motore si **rinominano solo le stringhe degli esiti** in `conteggioDefinitivo` (nessuna modifica di logica; aggiornato anche l'array `ESITI` del driver `test/partita_smart.js`). Terminologia: si dice **piani di lettura**, non "assi". Propagato a KB §24/§26 (+ §26.1/§26.2) e registro v1.16, manuale cap. 8 e cap. 9 (riscritto: 9.1 due piani, 9.2 quattro esiti, 9.3 domande, 9.4 epilogo), `REGOLE`, `REGOLE_IA` e motore in `index.html`. **Editoriale (solo manuale):** rimossa la vecchia sezione "Leggere il piatto: la storia dietro i numeri" (era §9.2), giudicata da Saverio ridondante con §5.10 (narrare l'esito / seme dominante); bonificati i rimandi (riga su "vedi Capitolo 9", domanda 9.3). Il **diario del rapporto** (♥ vs ♠) resta un meccanismo del gioco nella KB (§27.2) e nell'UI, semplicemente non più ri-spiegato nel capitolo finale del manuale.

41. **Capitolo 1 riscritto come "panoramica generale", Capitolo 2 rimosso, capitoli rinumerati (solo manuale, giu 2026)**: Saverio ha riscritto il Capitolo 1 del manuale (`docs/frenemies_manuale.md`) come panoramica generale completa, con nuove sezioni §1.6 "Come si gioca", §1.7 "Competizione e Collaborazione" (l'espediente meccanico al servizio della narrazione emergente, più l'invito a essere generosi sugli "sconfinamenti" narrativi tra i lati) e §1.8 "Come finisce?" (anticipa i due piani missione/prestigio e i quattro esiti). Poiché il nuovo Cap. 1 ingloba materiali, due mazzi, cinque scene e come-si-gioca, il vecchio **Capitolo 2 "Quello che ti serve" è stato rimosso** perché ridondante, e tutti i capitoli successivi sono **scalati di uno** (3→2 … 10→9) con i relativi paragrafi (es. §5.x → §4.x) e rimandi interni (incluso "Capitolo 5"→"Capitolo 4", "§5.10"→"§4.10", e il "vedi Capitolo 2" sul titolo → "Capitolo 1"). Rinumerazione fatta con script una tantum (`scratchpad/renumber_ch.py`), indice aggiornato. Nessun effetto su KB, motore o regole: è solo l'impaginazione del manuale (documento derivato). NB: i riferimenti a capitoli/paragrafi del manuale nelle decisioni precedenti (#39 "manuale §5.6", #40 "manuale cap. 8 e cap. 9 / §9.x") si riferiscono alla numerazione PRECEDENte: dopo questo cambio §5.6 → §4.6 (le tre mosse) e il finale è ora il Cap. 8 (§8.1-8.4). I nomi degli esiti in §1.8 sono stati allineati a quelli decisi in #40 (Vittoria piena / Per il rotto della cuffia / Sconfitta dignitosa / Fallimento totale).

42. **Competizione e collaborazione + "sconfinamenti" narrativi propagati, e fix punteggio resa (giu 2026)**: il principio introdotto nel manuale §1.7 (la competizione meccanica è un espediente al servizio della narrazione emergente, sotto resta un gioco collaborativo; e gli **sconfinamenti** narrativi sono incoraggiati: chi gioca l'Opposizione/Fiori può mostrare difetti e debolezze dei protagonisti, chi gioca i Protagonisti può far innamorare/impietosire/convincere/mettere al tappeto un PNG dell'Opposizione; si costruisce sempre sugli elementi introdotti dagli altri) è stato propagato a KB §5 (paragrafo "Competizione e collaborazione"), a `REGOLE` (sezione "I quattro semi", nuovo blocco) e a `REGOLE_IA` (guida alla narrazione) in `index.html`. Nello stesso passaggio corretto un residuo stale nel manuale: la **Resa onorevole** indicava ancora "1 punto, o 2 se è una figura" → ora "1 punto, anche se è una figura" (coerente con v1.15 e con la KB §18.1). Le occorrenze "presa con figura 2 / scopa 4" che restano nel manuale sono dentro l'**esempio canonico (Appendice B)**, già marcato come da rigenerare, e non vanno toccate a mano.

43. **Jolly semplificato: il «peccato emerge» diventa licenza puramente narrativa (§22.5/§22.6, v1.25)**: scelta del designer (Saverio, giu 2026) per snellire il Jolly. (a) **Rimosso il terzo uso meccanico del marcatore** (la v1.5): l'Opposizione non spende più il marcatore come azione aggiuntiva per togliere dal piatto la carta più bassa dei Protagonisti. Il marcatore torna a essere una risorsa normale (punti al conteggio, spendibile al mercato). (b) **Il peccato resta solo in chiave narrativa**: finché l'Opposizione **possiede il marcatore del Jolly** (predicato `jollyMarkerO()`, scelta «solo finché lo possiede»: se lo spende al mercato la licenza decade), in una sua qualsiasi presa o scopa può far emergere nella narrazione il compromesso dei Protagonisti, per dividerli e drammatizzare; non cambia punti né carte. Precisato il vincolo narrativo §22.4: a macchiarsi del peccato è **uno solo dei due protagonisti, all'insaputa dell'altro** (ricaduta nei vecchi vizi); propagato a KB §22.4/§22.6, manuale §5.2/§5.4, `REGOLE`/`REGOLE_IA`, hint di `suggerimento` e `istruzNarrBase`. (c) **Scarto volontario del Jolly**: chi lo ha in mano può «resistere alla tentazione» e scartarlo senza effetto anche con carte da catturare (prima lo scarto era solo il caso forzato del piatto vuoto). **Motore `index.html`:** rimossi `peccatoEmergeDisponibile`/`eseguiJollyEffetto`, il pulsante `#btnPeccato` in `rTurno` (+ listener), la chiamata IA in `iaTurno` e la voce `tipo:"peccato"` di `mossaInfo`; `jollyMarkerO()` resta come predicato della licenza; nuovo helper `scartaJolly(c,motivo)` con il bottone «Resisti alla tentazione» in `opzioniJolly`; licenza narrativa aggiunta in `suggerimento` (ramo lato O) e `istruzNarrBase` (prompt narrazione IA). Testi `REGOLE`/`REGOLE_IA` aggiornati. **KB:** §22.5 (scarto/resistere) e §22.6 (peccato narrativo) riscritti, esempio §22.6 portato su Vera/Otto, registro v1.25 + footer. **Manuale:** §5.2 (resistere alla tentazione) e §5.3 (peccato narrativo) riscritti, glossario «Marcatore del Jolly» aggiornato. **Test:** rimosso `test/test_peccato.js` (collaudava il meccanismo soppresso; non era nella lista di collaudo di `CLAUDE.md`). **Esempio canonico invariato:** il transcript seed 23/43/5 non usa l'effetto rimosso e i punti del Jolly non cambiano, quindi §34/Appendice B e il transcript di riferimento restano com'erano.

44. **Il sacrificio di una figura vale 1 punto, mai scopa (§15.3/§16, v1.26)**: segnalazione di Saverio (giu 2026), nata scrivendo l'Appendice B (perché Paola "buttava" il Fante nel piatto vuoto per una scopa?). Prima una figura giocata sul **piatto vuoto** si sacrificava e valeva una **scopa** (il «gesto eroico», v1.6/v1.23). Incoerente con la definizione di scopa: **una scopa è sempre e solo una presa che svuota il piatto**, e una figura che non cattura nulla non ne fa una. Ora il sacrificio vale **sempre 1 punto**, come una **resa onorevole** (la figura va tra le prese come marcatore da 1, lasciando sul tavolo le carte fuori portata); l'effetto speciale scatta comunque. Sparisce anche l'incentivo a farmare 3 punti garantiti scaricando una figura nel vuoto. Propagato: motore `index.html` (ramo `sacrificio` di `giocaCarta` → prese/1 punto, mai `aggiungiScopa`; testi e bottoni di `opzioniFigura`; `REGOLE`/`REGOLE_IA`), KB §15.3/§16 (+registro v1.26 e footer), manuale §3/§6/glossario, driver `test/partita_smart.js` (la figura sul piatto vuoto non vale più 1000 ma il punteggio basso del sacrificio). **Esempio da rigenerare:** col motore corretto cambia il gioco ottimale da S3 in poi; S1/S2/mercato del seed 34 (nessun sacrificio) restano validi, S3-S5 e la scelta del seed vanno rifatte.

45. **Euristica anti-spreco figure nel driver `test/partita_smart.js` (solo tooling, non è una regola)**: sotto la v1.26 l'IA del driver scaricava una figura come **sacrificio da 1 punto** anche quando, giocandola prima, avrebbe potuto fare una **presa vera** (es. seed 34/1 in S4: la Regina poteva catturare il 4♣ ma il driver giocava prima il Fante e lasciava la Regina incastrata sul piatto vuoto). Causa: il punteggio della presa con figura era identico a quello della presa con un numero, senza preferenza per incassare la figura adesso. Fix: alla presa con figura (ramo non-scopa di `scegliMossa`) si aggiunge un bonus `+FIGVAL[c.fig]`, così una presa fatta con una figura batte la stessa presa fatta con un numero (un numero si può buttare sul piatto senza danno più tardi, una figura no, rischia il sacrificio da 1 pt) e, tra due figure, si cala prima la **più alta** (la più dolorosa da sprecare). Migliorata anche la ricerca a batch: la modalità `FIGJOLLY` ora distingue se Regina e Fante fanno **presa vera** o **sacrificio da 1 pt** (`reginaPresa`/`reginaSacr`/`fanteSacr`), rilassato il vecchio malus "figura nei colpi" (l'ingresso di Pablo nei colpi è ora un'azione significativa, non una penalità), e aggiunto `BATCHSTART=N` per scandire il residuo dei seed oltre i primi ~300 (tetto memoria JSDOM). Nessun effetto su regole, motore o `test/partita_esempio.js`. **Seed-vetrina S3-S5 scelto: `34/367`** (seed 34 per S1-S2, SEED2=SEED3=367 per il mazzo residuo): arco **POPOP**, Fante con **SCAMBIO** 3♠↔10♥ in S3, **Jolly SCOPA-sleale** in S3, Regina presa in S4, Re presa in S5, ribaltone dopo i colpi, esito **Vittoria piena** 26-5 (nei seed 1-580 non esiste un POPOP che tenga insieme cuffia + effetto Fante visibile senza sprecare una figura: scelto da Saverio lo showcase completo invece della cuffia). **Prossimo:** scrivere la fiction di S3-S5 ancorata a `transcript_smart_seed_34_367.txt`.

46. **Jolly: il peccato non è più «all'insaputa dell'altro», ma una colpa che l'altro può vedere e giudicare (revisione del vincolo narrativo §22.4)**: scrivendo l'Appendice B (Scena 3, ⑧: Vera fa franare il canyon sugli inseguitori con la dinamite, e Otto la vede e la giudica) Saverio ha generalizzato il vincolo narrativo del Jolly fissato in v1.25 (#43). Prima il peccato era **sempre segreto** («uno solo dei due protagonisti, all'insaputa dell'altro»); ora a macchiarsene è sempre **uno solo dei due**, ma può essere **una ricaduta nei vecchi vizi oppure un gesto avventato che non tiene conto dell'altro**, e **l'altro può non saperne nulla, oppure assistervi e giudicarlo**. Aggiunta l'immagine del designer: «il Jolly è il seme di una discordia destinata a sbocciare più avanti». Di conseguenza il «peccato emerge» (§22.6) si riformula: l'Opposizione, finché possiede il marcatore, **fa riaffiorare** quel peccato per **far sbocciare la discordia** (non più solo «rivelare un segreto»). Propagato: manuale §5 (spiegazione del Jolly + sezione «il peccato emerge») e glossario; KB §22.4 e §22.6; `index.html` REGOLE (Vincolo narrativo + Il peccato emerge), REGOLE_IA (blocco JOLLY + hint del bottone "peccato emerge"). Lasciato com'è il **registro storico v1.25** in KB (documenta la formulazione di allora). Nessun effetto su meccanica, punti o motore: è solo l'interpretazione narrativa. Coerente con l'⑧ della Scena 3 dell'Appendice B (seed 34/367).

47. **Re (v1.27): l'effetto non è più uno scambio ma un recupero che aggiunge una carta al mazzo o alla riserva**: su richiesta del designer (Saverio) il Re smette di "riordinare la mano" e diventa un vero deus ex machina che **aggiunge una risorsa**. Effetto nuovo: quando giochi il Re, **recuperi dagli scarti comuni una carta del suo stesso seme** (quello del Re giocato, non più entrambi i semi del lato) **senza togliere nulla dalla mano**; nelle **scene 1-4** la carta recuperata si **rimescola nel mazzo** di chi gioca il Re, nell'**ultima scena** entra nella sua **riserva** (alimenta i colpi di scena). Chi gioca sceglie la carta (l'IA prende quella di valore più alto); se negli scarti non c'è alcuna carta di quel seme, l'effetto non fa nulla. L'effetto scatta comunque, anche se il Re si sacrifica (piatto senza bersagli). Questo rende il recupero del Re una **nuova eccezione** alla regola "non si rimescolano mai gli scarti nel mazzo" (accanto a figure comprate, Ritirata strategica e ingresso del Jolly). Propagato nello stesso commit: `index.html` (ramo `c.fig==="R"` di `effettoFigura` con logica recupero+rimescolo/riserva e modale «Recupera», `FIGURE.R.eff`, stringhe `G.ultimaGiocata`, tabella figure in `REGOLE`, `REGOLE_IA` ed effetto del Re nel prompt del mercato per l'IA); KB §21 (effetto), §16 (eccezione al rimescolo), registro **v1.27** e footer di versione; manuale §1.5 (eccezioni al rimescolo), descrizione del Re in §3 e voce di glossario (NON toccata la fiction dell'Appendice B); driver `test/partita_smart.js` (auto-risoluzione del Re che recupera il seme più alto + euristica che **trattiene il Re per la presa più grande**, somma fino a 10, evitando sacrifici e prese piccole). L'esempio canonico (seed 34/367) è stato **rigenerato**: scene 1-4 identiche, scena 5 modificata perché ora il Re di Quadri di Omar recupera un ♦ alto nella propria riserva. *(Fix di tooling collegato, scoperto qui: nei colpi di scena il driver `test/partita_smart.js` cliccava sempre l'ultimo bottone, cioè «Metti senza eliminare» — anch'esso con attributo `data-bersaglio` ma vuoto — quindi non usava MAI l'eliminazione (§24). Corretto: ora filtra i bersagli reali e colpisce quello di valore più alto. Il transcript della cripta ora mostra l'eliminazione da entrambi i lati: Paola 7♥ elimina 5♦, Omar 10♦ elimina 7♥, poi 8♠ e 9♠ si aggiungono; piatto finale 2♠ 10♦ 8♠ 9♠, Protagonisti 19 a 10, Vittoria piena. Non è una regola, solo il driver che ora gioca i colpi correttamente.)*

48. **Colpi di scena a turni strettamente alterni, niente colpi consecutivi (§24, v1.28)**: scelta del designer (Saverio, giu 2026). Prima (v1.3) i colpi di scena si alternavano finché entrambi avevano riserva, ma se un lato restava senza carte l'altro **proseguiva da solo** giocando tutta la riserva di fila, e la fase finiva solo a riserve entrambe esaurite. Ora i colpi si **alternano strettamente** e **nessun lato gioca due colpi consecutivi**: dopo il colpo di un lato tocca all'altro; se l'altro non ha più riserva la fase **finisce subito** (conteggio definitivo) e non si torna indietro a far rigiocare il primo. Conseguenza: chi inizia (il lato che perde il primo conteggio) gioca al massimo una carta in più dell'avversario, e l'eventuale riserva in eccesso resta inutilizzata. Resta vero che, nel proprio turno, finché hai riserva devi giocare una carta (gioco obbligatorio) e che si può eliminare una carta avversaria di valore ≤ (§24 invariata su questo). Motivazione: lo scambio finale resta un vero botta e risposta a turni, non un monologo di chi ha messo da parte più carte. Propagato nello stesso commit: `index.html` (`prossimoColpo`, rimosso il ramo che faceva proseguire lo stesso lato; banner del primo conteggio; `REGOLE`; `REGOLE_IA`); KB §24 (alternanza/fine + nota sull'esempio), registro **v1.28** e footer; manuale cap. 8 (colpi di scena) e glossario (NON toccata l'Appendice B). **Driver `test/partita_smart.js`:** nuova euristica dei colpi (giocando meno carte la scelta conta): se c'è un bersaglio eliminabile, si colpisce l'avversaria di **valore più alto** eliminabile con la carta di riserva **più bassa** che la elimina (val ≥ val bersaglio, così si conservano le carte alte); se non c'è alcun bersaglio, si gioca la carta di riserva **più alta** senza eliminare. L'esempio canonico `transcript_smart_seed_34_367.txt` è stato **rigenerato**: scene 1-4 identiche; la scena 5 (cripta) ora chiude con Paola 7♥ elimina 5♦, Omar 10♦ elimina 7♥, Paola 9♠ (l'8♠ resta inutilizzato in riserva, perché Omar non ha più carte e Paola non gioca due colpi di fila), piatto definitivo **2♠ 10♦ 9♠**, Protagonisti **11** contro Opposizione **10**, **Vittoria piena**, diario su, su, pari, pari, giu.

49. **Tie-break del conteggio definitivo: decide la Crescita (§25, v1.29)**: scelta del designer (Saverio, giu 2026). Prima, in caso di **pareggio** del piatto finale, il tie-break contava prima le scope e poi le prese. Ora decide la **Crescita** (i punti non spesi al mercato): il lato che ne ha di più la **spende tutta** (i suoi punti vanno a 0) per aggiudicarsi il piatto e quindi la missione, nonostante il pareggio; se la Crescita è **pari** (anche 0-0) il piatto va comunque ai **Protagonisti**. Quando il pareggio lo vincono i Protagonisti (sia spendendo la Crescita, sia per parità), l'esito è forzato a **«Per il rotto della cuffia»**: vincono di strettissima misura. Motivazione: in un pareggio è la riserva di crescita non spesa a fare da ago della bilancia, e chi se la gioca tutta paga la vittoria con la propria crescita. Propagato nello stesso commit: `index.html` (`rFinale`: rimosso il tie-break scope→prese, ora confronto della Crescita con azzeramento del lato che la spende ed esito forzato; `REGOLE` e `REGOLE_IA`); KB §25 (tie-break) e §26 (nota «Parità ai punti»), registro **v1.29** e footer; manuale §6.3 e glossario «Conteggio definitivo» (NON toccata l'Appendice B). **L'esempio canonico non cambia** (seed 34/367, 11-10: niente pareggio, la v1.29 non scatta). Collaudo verde.

50. **Il §34 della KB diventa un rimando all'Appendice B del manuale (non più un riassunto)**: scelta di Saverio (giu 2026). Il §34 era ancora il vecchio riassunto sul seed 23/43/5 (12-0 al piatto, 2-8 ai punti, Vittoria per il rotto della cuffia, diario su/pari/pari/su/giù), con tanto di banner «⚠️ da rigenerare per la v1.15». Ora che l'actual play narrato completo vive nell'**Appendice B del manuale** (seed 34/367), il §34 non duplica più il riassunto: è un **rimando** all'Appendice B con un «in breve» di tre righe (arco POPOP, diario su/su/pari/pari/giù, vetrina delle tre figure + Jolly + tre spinte + colpi, ribaltone finale, **Vittoria piena** 11-10, Crescita 5-2). Così si evita il drift tra KB e manuale. Aggiornato anche il cross-ref inline «Vedi gli acquisti narrati nel §34» (§21 KB) → «nell'Appendice B del manuale». Il §33 (scenario demo) era già su Vera/Otto e non cita dati numerici stale: invariato. NB: restano fuori da questa fase gli **esempi inline ancora su Frank/Skunk** sparsi nella KB (§ vari) e i blocchi `.es`/tono in `index.html` (sweep separato, vedi note del piano).

51. **Il conteggio dei punti dell'Appendice B era errato: il mercato può sovrappagare (corretto sul motore)**: scoperto rifinendo la Fase 3 (giu 2026). Riscrivendo il §34 ho confrontato i numeri dell'Appendice B (manuale) col transcript canonico e col motore: divergevano. La Crescita finale dei Protagonisti era narrata **6**, ma motore e driver danno **5**. Causa: `pagaCosto` (`index.html`) spende i marcatori **prima le prese (1), poi le scope (3) intere**, e una scopa **non dà resto**; se il costo non si copre con le sole prese, si spezza una scopa **sovrappagando**. Nell'esempio Paola, con 8 punti (2 prese + 2 scope), compra il **Fante (costo 3)** spendendo 2 prese + 1 scopa intera = **5 effettivi** (spreca 2), e le restano **3** non 5; la **Regina (costo 5)** invece si paga esatta (2 prese + 1 scopa). Catena reale: 8 → Fante → 3 → +5(S3) = 8 → Regina → 3 → +1(S4) = 4 → +1(S5) = **5**. Omar: 8 → Re (costo 8, esatto) → 0 → +2(S5) = **2**. Esito invariato: **Vittoria piena**, piatto 11-10. Decisione di Saverio: **il motore è la fonte di verità**, si correggono i numeri del manuale (non il codice). Corretti in `docs/frenemies_manuale.md` (Appendice B): i due passaggi di mercato (Fante «le restano 3» con spiegazione dello spreco come lezione didattica; Regina «le restano 3», pagamento esatto), il mercato S4→S5 («i quattro che le restano» / «tiene i quattro punti») e il finale (Crescita 5-2 in due punti). **Loose end:** valutare se rendere esplicito anche in §22 (mercato, KB/manuale) che «le scope non danno resto» (oggi è solo implicito nell'ordine bonus→prese→scope); per ora la lezione è illustrata inline nell'Appendice B.

52. **Appendice G (schede da fotocopiare): foglio del tavolo e carte dei Qualcuno semplificati**: richiesta di Saverio (giu 2026). Solo manuale (`docs/frenemies_manuale.md`), nessun effetto su regole o app. (a) **Foglio del tavolo:** il campo **Tono** ora è vuoto (tolta la lista prescrittiva «Action · Poliziesco · Comedy · Dramedy · Romance · libero») e accanto è stato aggiunto un campo **Genere**, anch'esso vuoto: due campi liberi da compilare a penna, in tre colonne con «I protagonisti». (b) **Carte dei Qualcuno:** ogni blocchetto ha ora solo **Nome, Descrizione, Carta**; rimossi la checkbox «catturato», la voce «Cosa vuole (la sua volontà)» (rinominata in «Descrizione») e il blocco dei tipi (checkbox PNG / Figura / Gruppo·popolo / Luogo·oggetto + «Carta che lo rappresenta»). Rigenerato `docs/frenemies_manuale.pdf`.

53. **Campo «Genere» nel setup del gioco digitale (allineamento all'Appendice G)**: richiesta di Saverio (giu 2026), per portare nell'app la modifica #52. Scelta (tra tre opzioni): i pulsanti preset del **Tono** restano invariati, si **aggiunge** accanto un campo **Genere** a testo libero (facoltativo, vuoto di default). Nuovo stato `G.genere` (default `""`), input `w-genere` nel passo «Tono» del wizard, letto da `salvaCampi()` e persistito anche cliccando un pulsante Tono (aggiunto `salvaCampi()` nell'handler). Il Genere è **inviato a Claude** come contesto accanto al Tono (`statoPerIA`), incluso nell'export/import del playset (`ps.genere`) e mostrato accanto al Tono nel riepilogo del pitch, nell'header in gioco e nel cartello. Non è una regola: nessun effetto su REGOLE/REGOLE_IA/KB/manuale del regolamento. Collaudo verde (test_partita NG2/NG4, adatta, solo, riprova).

54. **I punti sono una valuta lineare: al mercato si tiene sempre il resto (§20, v1.31)**: segnalazione di Saverio (lug 2026), giocando: con **4 punti** (1 presa + 1 scopa) comprando il **Fante (costo 3)** ci si ritrovava a **0** invece che con il **resto di 1**. Chiarimento del principio, dal designer: **i punti sono una valuta lineare**; i marcatori (prese 1, scope 3) sono solo il modo pratico di segnarli (si potrebbero usare gettoni o scrivere su un foglio). Quindi il mercato **dà sempre il resto**, anche spezzando una scopa: 2 scope (6) per una Regina (5) → **resto 1**; 1 presa + 1 scopa (4) per un Fante (3) → **resto 1**. Causa del bug: `pagaCosto` spendeva rigidamente **bonus → prese → scope** e la scopa spezzata **non dava resto** (artefatto dell'ordine di spesa, non una scelta di design). Questo comportamento era stato **documentato come intenzionale in #51** («il motore è la fonte di verità, si correggono i numeri del manuale»): la #54 **rovescia #51**. *(Nota di percorso: in una prima iterata avevo proposto e implementato un «pagamento ottimale senza resto» — paga esatto quando può, scope spezzate solo se necessario, eccedenza comunque persa — scelto via AskUserQuestion; Saverio ha poi chiarito il principio della valuta lineare, quindi il resto va dato **sempre** e il caso «scope non danno resto» sparisce del tutto.)* Nuovo `pagaCosto`: copre il costo spendendo i marcatori con **meno spreco possibile** (per conservare i semi di quelli che restano) e l'eventuale **eccedenza di una scopa spezzata torna come punti `bonus`** (valuta senza seme); il totale che resta è sempre esattamente `punti − costo`. Propagato nello stesso commit: `index.html` (`pagaCosto`, testo del pannello mercato «i punti sono una valuta … tieni sempre il resto»), KB §20 + registro **v1.31** + footer. `REGOLE`/`REGOLE_IA`/`setupPerIA` non descrivevano l'ordine di spesa dei marcatori (solo l'ordine compra→mescola→pesca e «presa 1 / scopa 3»), quindi non toccati. **Impatto sull'esempio canonico:** la modifica cambia **quali** marcatori finiscono negli scarti comuni (e il consumo di RNG per il rimescolo), quindi a cascata l'effetto Fante/Re e l'intero finale del seed 34/367. L'Appendice B del manuale (fiction curata, ancorata al transcript) e i numeri di #51 (Paola Fante «restano 3» + «lezione dello spreco», che ora **non ha più senso**: il resto c'è sempre) sono **obsoleti e da riscrivere** nella finalizzazione già pianificata (`docs/piano_finalizzazione_esempio.md`); il transcript era comunque già fuori sync a HEAD (dava «cuffia su,su,su,su,giu», non l'«11-10 su,su,pari,pari,giù» dell'Appendice B), quindi la rigenerazione era già dovuta. **NON toccata la fiction dell'Appendice B in questo commit.** Collaudo core verde.

## 2. Interpretazioni implementative da validare al playtest

Punti dove il regolamento tace e il codice ha adottato una scelta. Funzionano, ma vanno confermati o corretti giocando:

1. **Pagamento al mercato automatico**: l'app spende nell'ordine punti bonus → prese numeriche → prese con figura → scope; i marcatori spesi vanno negli scarti comuni. Alternativa possibile: selezione manuale di cosa spendere.
2. **Spinte del pitch e Jolly**: le prese fatte col Jolly **non** sono potenziabili con una spinta del pitch (la spinta richiede una presa normale; il Jolly ha già il suo regime, cioè il punto regalato all'avversario).
3. **Parità nei totali del piatto a fine scena 1–4**: posta "sospesa", nessun vincitore, nessun recupero. Caso non coperto dal regolamento.
4. **Punti complessivi per l'outcome (§26)**: stessa formula del mercato (v1.6) — presa numerica 1, presa con figura 2, scopa 4 — e ciò che è stato speso al mercato vale 0 (contano solo i marcatori non spesi). Nel motore `punti()` finale richiama `puntiMercato()`, quindi mercato e conteggio finale sono identici: non è più un'interpretazione aperta ma una regola esplicita in §17/§20.
5. **Colpi di scena, chi inizia**: il lato che sta **perdendo** al primo conteggio (in parità, i Protagonisti). Il regolamento non specifica.
6. **(Superato) Counter del Jolly**: il counter e la schermata del difensore sono stati rimossi in v1.3 (voce "Jolly ridisegnato" in §1); il Jolly ora regala semplicemente il punto all'avversario, senza alcuna risposta del difensore. Diverso dal **terzo uso del Jolly** aggiunto in v1.5 (§22.6, voce 23 in §1): quello non blocca la cattura sul momento ma è una resa dei conti differita, che l'Opposizione spende in un proprio turno per togliere una carta dei Protagonisti dal piatto.
7. **Jolly su piatto vuoto**: viene scartato senza effetto e il piatto resta vuoto (§22.5; voce "Jolly ridisegnato" in §1).
8. **Recupero dei Protagonisti alla scena 5**: tecnicamente avviene anche lì se vincono (irrilevante a fine partita, ma il codice lo applica per coerenza).

## 3. Architettura del codice

- **Un solo file** `index.html`: HTML + CSS + JS, tutto client-side, nessun backend.
- **Stato puro separato dall'interfaccia**: l'oggetto globale `G` contiene tutto lo stato della partita; le funzioni `r*()` disegnano le schermate leggendolo. Regola ferrea: **le funzioni di render non devono mai mutare lo stato** — requisito indispensabile per l'online, dove lo stato viene replicato tra due client e un render "impuro" applicherebbe le mutazioni due volte.
- **Macchina a fasi**: `modalita → lobby (solo online) → setup (wizard 10 passi) → [per scena: asta → asta_rivela → apertura → turno/narrazione → fine_scena → mercato (→ jolly_intro dopo la 2ª) (→ mano_estesa/neutralizza prima della 5ª)] → primo_conteggio → colpi → finale`.
- **Pass-and-play**: velo di handoff tra i turni (mani coperte). **Online**: niente velo, ogni client vede solo la propria mano; gating per fase tramite `latoCheAgisce()`; replica simmetrica dello stato via PeerJS (chi agisce muta e trasmette l'intero `G`).
- **Online, scelte specifiche**: codice stanza di 5 lettere; mercato sequenziale (prima Protagonisti, poi Opposizione) per evitare conflitti di scrittura concorrente; il mazzo del compratore viene rimescolato subito a ogni acquisto (equivalente al rimescolo di fine fase, ma sicuro tra due client).
- **Salvataggio**: export/import dell'intero stato come JSON (niente localStorage: vietato negli artefatti; su Netlify si potrà aggiungere il salvataggio automatico).
- **Operazioni sulle carte sempre per id**, mai per riferimento: la serializzazione di rete rompe l'identità degli oggetti.
- **Modalità solo, contro Claude** (giugno 2026): terza modalità accanto a locale e online; l'umano guida i Protagonisti, Claude gioca e narra l'Opposizione via API Anthropic (negli artefatti la chiamata non richiede chiave; per Netlify servirà una chiave dell'utente o una function proxy, da decidere al deploy). Architettura: `iaScheduler()` agganciato in coda a `render()` agisce quando tocca all'Opposizione (asta, apertura, turno, counter, narrazione con eventuale spinta, mercato, mano estesa, colpi, modali figura). Le decisioni passano dalle **mosse legali calcolate dal motore** (`combosPresa`, `carteGiocabili`, ecc.) inviate come elenco numerato; la risposta è JSON (`{"scelta": n}` e simili) e **ogni risposta assente o invalida ripiega su una scelta automatica**: l'IA irraggiungibile non blocca mai la partita. Segretezza: il prompt contiene solo ciò che l'Opposizione ha diritto di sapere (mai la mano dei Protagonisti); le schermate con la mano di O hanno classe `.ia-nascosta`; il counter con difensore O mostra una schermata neutra e si risolve via `risolviJolly()` senza rivelare se l'IA ha il Jolly. Le narrazioni di Claude appaiono nel pannello della narrazione (`#iaNarr`), confermate dall'umano. Velo disattivato in solo. Rifinitura nota: l'effetto figura dell'IA (Fante/Re) è risolto con il default e il modale può lampeggiare un frame.
- **Log narrativo condiviso con Claude** (giugno 2026): in modalità solo Claude poteva narrare e decidere ma non "ricordava" la storia, perché `iaContesto()` passava solo lo stato delle carte e la narrazione umana non veniva mai catturata (l'umano leggeva il suggerimento e cliccava "Narrato"). Aggiunto `G.storia` (array append-only `{scena, lato, testo}`, parte di `G` così sopravvive a export/import). Cattura, solo se `G.modalita==="solo"` via `aggiungiStoria(lato,testo)`: apertura di scena (titolo + posta, nell'handler di `a-via`), narrazione di Claude (in `iaNarrazione`, accanto al box `#iaNarr`), narrazione umana (nuovo `textarea#narrInput` in `rNarrazione`, bozza in `G.narr.bozza` per sopravvivere ai re-render da spinta/PNG). In solo la narrazione umana è **obbligatoria**: `narrOk` è disabilitato finché il campo è vuoto. `iaContesto()` ora appende l'intera storia (`storiaTestoIA()`). Colonna verticale fissa a destra (`aside#storiaCol`, accanto a `<main>` dentro il nuovo wrapper flex `#corpo`), con scrollbar propria per rileggere le scene precedenti: `aggiornaStoriaLog()` in coda a `render()` aggiorna `#storiaLog` e fa auto-scroll in fondo solo se l'utente era già vicino al fondo (altrimenti ne rispetta la posizione di lettura). `adattaViewport` continua a misurare solo `<main>`, quindi la crescita del log non rimpicciolisce il gioco. La colonna compare solo in solo durante le fasi di gioco. Tutto inerte fuori dal solo (locale/online invariati). `test_solo.js` aggiornato: scrive in `#narrInput` prima di confermare e verifica che il log contenga voci sia P sia O.
- **Canale di chiamata a Claude dentro l'artefatto** (giugno 2026): la `fetch` diretta verso `api.anthropic.com` è **bloccata dal sandbox CSP/iframe** degli artefatti Claude.ai, quindi falliva sempre e ogni decisione (inclusa la narrazione) ripiegava sul fallback: dentro Claude.ai si vedeva solo la frase di riserva, mai una vera narrazione. `chiamaClaude` ora usa `window.claude.complete(prompt)` quando disponibile (canale dedicato degli artefatti: una sola stringa in ingresso, niente selezione di modello né system prompt separato, tutto nel prompt) e tiene la `fetch` come fallback per ambienti fuori artefatto (proxy/chiave su Netlify) e per lo stub di rete di `test_solo.js`. Aggiorna la nota "negli artefatti la chiamata non richiede chiave" del punto precedente.
- **BYOK diretto + prompt caching + regolamento operativo** (giugno 2026): per dare a Claude il regolamento completo, il pitch, i PNG e la storia senza ripartire da zero a ogni mossa, e senza far pagare nessuno se non chi gioca. Niente proxy/backend: fuori dall'artefatto le chiamate vanno **diritte** ad `api.anthropic.com` dal browser con la **chiave del giocatore** e l'header `anthropic-dangerous-direct-browser-access: true` (sblocca il CORS). La chiave vive in una variabile di sessione `claudeKey` (mai in `G`, mai salvata/esportata, niente localStorage): si incolla nel campo della schermata "Contro Claude" o col pulsante 🔑 nella colonna della storia (`chiediChiaveClaude()` apre un `modale()`). Senza chiave, `chiamaClaude` ritorna `null` e l'IA ripiega sulla mossa automatica (così `test_solo.js` resta verde senza chiave). Il **prompt caching** (`cache_control:{type:"ephemeral"}` sul blocco `system`) vale anche sulle chiamate dirette. Modello fisso `claude-sonnet-4-6` (il più economico; cambiabile in un punto). `chiamaClaude` separa **system statico** (regole + setup, cacheato, byte-identico per partita) da **user dinamico** (storia + stato + istruzione); nell'artefatto resta `window.claude.complete(sys+user)`. Scelto un **regolamento operativo curato** (espansione di `REGOLE_IA`, ~2-3k parole derivate dalla costante in-app `REGOLE` v1.3) invece del KB intero: più economico su ogni chiamata, più nitido e robusto al TTL di 5 min della cache che le pause umane farebbero scadere. `iaContesto()` spezzata in `setupPerIA()` (statico: missione, tono, protagonisti, opposizione, prima difficoltà, pitch) e `statoPerIA()` (dinamico: scena, piatto, mano O, prese/scope, totali, figure, PNG). Sicurezza: nessuna chiave condivisa né proxy aperto; la chiave resta nel browser del giocatore (nota: lo script PeerJS da CDN è codice di terzi nella pagina, rischio remoto di esfiltrazione). Scartato il proxy Netlify (`ANTHROPIC_API_KEY` lato server) proprio per non far pagare il deployer e non esporre un endpoint spendibile.
- **Rifiniture esperienza vs Claude** (giugno 2026): (1) Setup autonomo dell'Opposizione: l'umano (Protagonisti) compila missione, protagonisti e le domande ♠♥ (0-5) + prima difficoltà; `generaOpposizioneIA()` fa scrivere a Claude `G.nemici`/`G.caos` e le risposte del pitch 6-9 (♦♣), mostrate poi in sola lettura al passo "Opposizione" con un "🔁 Rigenera" (vedi la voce dedicata in §4: rigenera con feedback facoltativo). Trigger non bloccante in coda a `render()` (solo se canale disponibile: `claudeKey||inArtefatto()`), guardato da `oppGenInCorso`/`G.oppTentata`/`G.oppGenerata`. Senza canale (o su fallimento) si ripiega sui campi manuali, quindi `test_solo.js` (senza chiave) resta verde. (2) Turno di Claude con schermata pulita: in `rNarrazione`, quando narra l'Opposizione in solo, vista in sola lettura (carta + riga sintetica `descrizioneMossaO` + narrazione in grande nello slot `#iaNarrSlot` + solo "Avanti"), senza pannello "Ora narra", pulsanti spinta, "Crea PNG" o textarea: la spinta la decide Claude e `iaNarrazione` la esegue con `eseguiSpinta(...)` diretto (niente più click su pulsanti DOM). L'iniezione mantiene l'id `#iaNarr` (così `test_solo.js` non cambia) e abilita "Avanti" a narrazione pronta. (3) Coerenza col seme: il prompt di `iaNarrazione` esplicita il seme appena giocato (♦ = nemici attivi, ♣ = caos/sfortuna). (4) Log a destra a blocchi: ogni voce ha una barra colorata per seme con giocatore, carta compatta (es. "9♦") e tipo mossa con icona (▭ elemento, ✋ presa, 💥 scopa, 🎬 apertura), e sotto il testo narrativo. Richiede metadati nella voce: `aggiungiStoria(lato,testo,{tipo,carta,seme})`, con `carta` compatta salvata in `G.narr.carta` (in `vaiNarrazione`).
- **Prompt di setup Claude (chiave + modello) e playset** (giugno 2026): (1) scegliendo "Contro Claude" si apre un modale `chiediSetupClaude(avvia)` con il **menu a tendina del modello** (`MODELLI_IA`: Sonnet 4.6 default, Opus 4.8, Fable 5, Haiku 4.5) e il campo chiave, al posto del campo sotto il bottone; lo stesso modale si riapre dal pulsante 🔑 (avvia=false). Il modello scelto sta in `claudeModel` (var di sessione) e va nel body della chiamata diretta. Dentro l'artefatto il modale segnala che chiave/modello non servono. `test_solo.js` aggiornato per cliccare "Inizia". (2) `PLAYSET` (array, primo: "Frank & Skunk" completo con missione/protagonisti/opposizione/10 risposte/tono/prima difficoltà) caricabile da un pulsante "📋 Carica un playset" al passo 0 del wizard (`caricaPlayset`/`chiediPlayset`), in ogni modalità; in solo `caricaPlayset` imposta `oppGenerata=true` (il playset fornisce già l'Opposizione, niente auto-generazione di Claude).
- **Formato playset standard + import/export** (giugno 2026): adottato come formato di scambio il JSON con chiavi `tono, missione, primaDiff, pers:{A,B:{nome,desc}}, nemici, caos, pitch:[{seme,domanda,risposta}], geografia_scene:[{scena,fase,titolo,suggerimento_posta}]` (più `playset`/nome, `nGiocatori`, `formato`), che mappa 1:1 sullo stato `G`/wizard (cartella `playsets/` come libreria di file condivisibili). Loader unico `caricaPlaysetObj(ps)` tollerante: accetta `playset` o `nome`, `pers:{A,B}` o `persA/persB`, `pitch` come stringhe o oggetti `{risposta}`, `geografia_scene` o `scene`; `geografia_scene` pre-suggerisce titolo/posta delle 5 scene (`G.scene[i]`); dopo il carico salta al riepilogo. `esportaPlaysetFile()` (pulsante "Salva come playset" nell'overlay Salva) genera il JSON standard del pitch corrente e lo **scarica come file** (Blob+download). Il selettore playset (passo 0) ora ha anche "📁 Carica da file (.json)" (FileReader). Nota tecnica: i playset ufficiali restano **incorporati** in `PLAYSET` (un artefatto o `file://` non può elencare/fetchare la cartella); i `.json` servono per condivisione/upload. Il built-in è stato sostituito con "Frank, Skunk e il jet di Vargas".
- **Campo `premessa` + schermata di scelta scenario** (giugno 2026): (1) aggiunto al pitch il campo narrativo `G.premessa` (contesto da leggere al tavolo prima di partire, nessun effetto di gioco). Compare nel wizard al passo "Missione" **sopra** l'obiettivo (`#w-premessa`), nella scheda Missione e nel riepilogo; viene salvato (`salvaCampi`), caricato (`caricaPlaysetObj`, chiave `premessa`) ed esportato (`esportaPlaysetFile`, prima di `missione`); inviato anche a Claude in `setupPerIA` (riga "Premessa:", solo se valorizzata). Il formato di scambio dei `playsets/*.json` lo prevedeva già: ora il loader lo legge. (2) Il selettore playset non è più una semplice lista: `chiediPlayset` mostra un layout **master-detail** (elenco dei titoli a sinistra, scheda con premessa/protagonisti/obiettivo a destra via `schedaPlayset`, stato `psSelez`) e un pulsante **"Avvia scenario"**. Dopo il carico il wizard **non salta più al riepilogo** ma si apre dalla pagina "Missione" (`PASSI_WIZ.indexOf("Missione")`), così si scorre tutto il pitch compilato in avanti. (3) Tutti e cinque i playset di `playsets/*.json` sono ora **incorporati** in `PLAYSET` nel formato di scambio uniforme (`playset, tono, premessa, missione, primaDiff, pers:{A,B:{nome,desc,detesta}}, nemici, caos, spinte, png?, geografia_scene`); allineati a mano (file unico, niente build). Frank & Skunk (scenario demo §33) resta senza `premessa`, coerente col suo `.json`.
- **Wizard: spinte e difficoltà su pagine separate + campi "chi è/detesta" più grandi** (giugno 2026): la vecchia pagina unica "Le spinte" è stata divisa in **tre** pagine del wizard; `PASSI_WIZ` ora ha 10 voci: `… Protagonisti(4), Opposizione(5), Spinte protagonisti(6), Spinte opposizione(7), Difficoltà iniziale(8), Pronti(9)`. La pagina 6 raccoglie le 4 spinte ♠♥ (`w-pitch-2..5`), la 7 le 4 spinte ♦♣ (`w-pitch-6..9`, in solo mostrate read-only quando le scrive Claude), la 8 la sola prima difficoltà (`w-primadiff`, textarea grande a 6 righe). Conseguenze: `ownerSetup` online assegna 6→P, 7→O, 8→P (la difficoltà è risposta condivisa, la imposta il lato Protagonisti); **rimossa** la logica `passoSub` dalla navigazione (le spinte erano spezzate in due sotto-pagine solo per l'online: ora le pagine sono fisiche, il passaggio del tablet avviene al cambio pagina). Il trigger di generazione dell'Opposizione in solo resta a `passo===5`. Nella pagina Protagonisti (4) i campi **"Chi è"** sono passati da 3 a 5 righe e i due **"Cosa detesta"** da `input` a `textarea` di 4 righe (si può scrivere molto). Nessuna modifica alle regole: le spinte restano 8 (due per seme) e la prima difficoltà non spuntabile. Transcript §34 invariato.
- **Tono "Poliziesco" + titolo dello scenario demo** (giugno 2026): aggiunto **poliziesco** tra i toni di riferimento (regola tocca: KB §9.1 lista indicativa, `TONI` nell'app). Lo scenario demo (§33/§34) prende il titolo proprio **"La strada della vendetta"** e il suo genere passa da *action* a **poliziesco** (protagonisti invariati, Frank e Skunk). Propagazione: KB §9.1 e §33 (titolo + tono) + nuova sezione **v1.4** nel registro modifiche; `index.html` (costante `TONI`; `PLAYSET[0].playset`/`tono`); `playsets/frank_skunk_playset.json` (`playset`/`tono`/`fonte`); `playsets/schede/playset_frank_skunk.md` (intestazione); `test/partita_esempio.js` seleziona ora il tono "Poliziesco". Il tono non compare nel transcript: §34 resta byte-identico. Poliziesco non ha note speciali nel pitch (a differenza di Romance).
- **Opposizione: nemici ♦ e caos ♣ da stringa a lista di voci** (giugno 2026): i campi `G.nemici` e `G.caos` passano da stringa unica ad **array di voci separate** (ognuna un attore o un elemento distinto), con un editor a lista nel passo "L'opposizione" del wizard (input a riga singola + "+" per aggiungere, "✕" per togliere) al posto delle due textarea. Motivo: nemici e caos sono di solito più attori o più imprevisti diversi (forze dell'ordine, un rivale, un opportunista; un guasto, il meteo, vecchi conti), e tenerli come voci separate li rende leggibili e gestibili uno per uno. Retrocompatibilità totale via helper `normalizzaLista(v)` (array ripulito; stringa singola in `[stringa]`; vuoto/null in `[]`), applicato a salvataggi e import JSON legacy, scenari demo e output di Claude. Propagazione: data-layer e UI (editor `editorLista`/`aggiungiOppVoce`/`rimuoviOppVoce` che mutano `G` e fanno `render()`, viste in sola lettura `listaLettura`), contesto e contratto JSON verso Claude (`setupPerIA` invia le voci unite con `;`; `generaOpposizioneIA` ora chiede e riceve array), i 5 scenari di `PLAYSET` (attori già presenti splittati in voci), i test (`test_riprova`/`test_solo` allineati). **Non è una modifica di regole**: le semantiche ♦/♣ e `REGOLE_IA` non cambiano; il KB §33 (prosa) già enumera gli attori, nessuna riscrittura. Transcript seed 13 invariato. Requisito di Saverio.

## 4. Interfaccia e convenzioni

- Estetica **road movie notturno**: asfalto scuro (#15161a), mezzeria gialla (#f5a623/#ffd23f), font Oswald (titoli) e IBM Plex Mono/Sans.
- Colori dei semi: ♥ #e4572e · ♠ #d8d3c8 · ♦ #4f7cac · ♣ #6a994e.
- Tutto in **italiano**.
- **Suggerimenti di narrazione** dopo ogni giocata (tipo di azione + significato del seme + casi speciali figura/Jolly), con i nomi dei protagonisti del pitch inseriti nei testi.
- **Regolamento contestuale**: pulsante "?" in alto che apre la sezione pertinente alla fase corrente, con navigazione a chip verso le altre otto sezioni.
- **Diario del rapporto**: strada SVG che sale (♥) e scende (♠), si compila da sola leggendo il piatto; accessibile sempre dal pulsante in alto.
- Wizard del pitch con pulsanti "🎲 Ispirami" che pescano da coppie e missioni della Knowledge Base.
- **Intestazione di scena persistente** (giugno 2026): nelle fasi di gioco vero e proprio (turno, narrazione, counter) un banner fisso in cima a `main` mostra sempre numero/fase, titolo della scena e posta dichiarata. Generato da `intestazioneScena()`, sola lettura di `G` (rispetta la regola dei render puri), inserito in testa a `rTurno`, `rNarrazione` e ai due rami di `rCounter`. Requisito di Saverio: durante la scelta delle carte e la narrazione titolo e posta non devono mai uscire dalla vista.
- **Schede sempre accessibili (overlay multi-tab)** (giugno 2026): pulsante "Schede" nella barra in alto (accanto a "?", Diario, Salva) che apre l'overlay `ovSchede`; `apriSchede(tab)` (sola lettura di `G`) presenta cinque tab, riusando la barra a chip `regole-nav`: protagonista ♠ (scheda + domande del pitch di seme picche), protagonista ♥ (scheda + domande di seme cuori), Opposizione (♦ nemici, ♣ caos + domande di seme quadri/fiori), Missione (obiettivo/posta del viaggio + prima difficoltà), PNG (le figure acquistate da `G.figureComprate`, con nome/titolo/effetto e lato che le possiede). Le risposte del pitch sono suddivise per seme: picche→♠, cuori→♥, quadri+fiori→Opposizione. Il tab attivo è ricordato in `schedaTab`. Disponibile in ogni fase; ogni tab senza dati mostra un avviso.
- **Registro dei PNG con due origini** (giugno 2026): la scheda PNG (tab dell'overlay `ovSchede`) raccoglie in due sezioni separate (a) le **figure acquistate** al mercato e (b) i **PNG narrativi**. (a) All'acquisto di una figura `chiediPng()` apre un modale che chiede Nome e Descrizione; la logica di acquisto è estratta in `acquistaFigura(l,f,s,nome,desc,b)` (pagamento, copia unica, inserimento nel mazzo, DOM in-place), con i metadati in `G.png["<fig><seme>"] = {nome,desc}`. L'IA in modalità solo NON passa dal modale: `iaMercato` chiama direttamente `acquistaFigura` con nome di default (il titolo della figura), così non resta bloccata sul modale. (b) Durante la **narrazione**, un pulsante "➕ Crea PNG" (visibile solo se la carta giocata ha un seme; assente per Jolly e per le narrazioni senza carta come il counter) apre `creaPng()`: il PNG viene associato al **seme della carta appena giocata** (`G.narr.seme`, impostato in `vaiNarrazione`) e salvato in `G.pngNarr=[{seme,nome,desc,scena}]`. Il lato proprietario si deduce dal seme (♥♠ Protagonisti, ♦♣ Opposizione). I PNG creati compaiono anche come chip di conferma sotto la narrazione.
- **Mercato prima della scelta della mano nella 5ª scena** (giugno 2026): prima dell'ultima scena l'ordine ora è mercato → mano estesa (prima era l'inverso). Così le figure acquistate entrano nel mazzo (e vengono rimescolate) PRIMA di scegliere le 4 carte, quindi sono tenibili per la Scena 5 — coerente con il principio "ciò che entra nel mazzo dev'esserci prima della pesca" già applicato al Jolly. `avviaMercato` con `preScena5` non pesca e va al mercato; alla chiusura del mercato (`m-fine`, `G.scena===3`) parte `avviaManoEstesa`; `fineEstesa`, completati entrambi i lati, chiama `avanzaScena`. La soglia della scelta è stata generalizzata da "mazzo 5–7" a "mazzo >4" perché l'acquisto può aumentare il conteggio: con più di 4 carte si sceglie sempre quali 4 tenere (il resto in riserva). Durante la scelta è ora visibile il **piatto** (con i totali ♥+♠ / ♦+♣), così si decide la mano vedendo cosa si porta dentro l'ultima scena. Transcript seed 13 invariato.
- **Pitch a turni in online** (giugno 2026): nel setup online ogni lato compila e vede solo le proprie schede del wizard, sfruttando il gating già esistente (`latoCheAgisce`→`spettatore`→`rSpettatore`, niente velo online). `ownerSetup()` assegna il passo: Protagonisti possiede il passo "I protagonisti" (p4) e le domande sui protagonisti, Opposizione il passo "L'opposizione" (p5) e le domande sull'opposizione; intro/giocatori/tono/missione/riepilogo restano all'host. La fase domande (p6) è divisa in due metà tramite il sotto-indice sincronizzato `G.passoSub` (0=protagonisti+prima difficoltà, 1=opposizione); l'avanti/indietro gestiscono il passaggio tra le due metà e da/verso il riepilogo. Il lato non di turno vede `rSpettatore` ("Tocca a…"). In locale e solo nulla cambia: `passoSub` è inerte (`ownerSetup` ritorna subito l'host) e il wizard resta a schermata unica con entrambe le metà. Limite noto: online è 2 giocatori, quindi la divisione P/O coincide coi due giocatori; la prima difficoltà (condivisa nelle regole) la compila il giocatore Protagonisti, da concordare a voce.
- **Riassunto della scena nel diario** (giugno 2026): nella schermata di fine scena (`rFineScena`, scene 1-4), dopo la lettura del piatto, un campo testo permette di riassumere come si è chiusa la scena. Il testo si salva in `rec.riassunto` al click su "Vai al mercato/finale" (nel gestore, non nel render) e viene mostrato in corsivo tra le voci del diario (`apriDiario`). Campo aggiunto all'inizializzatore del record scena.
- **Spinte disponibili accanto alla mano** (giugno 2026): nella fase di turno, sopra le carte, una riga di chip mostra le spinte del pitch ancora spendibili dal lato di turno. `spinteResidue(l)` (sola lettura) filtra le risposte scritte di seme del lato, non ancora usate e con il seme non ancora bruciato (`G.spinte[l][seme]`), e restituisce vuoto nell'ultima scena (dove le spinte sono vietate). Diverso da `spinteDisponibili(l)`, che richiede una presa appena fatta (`boostCard`) per l'attivazione immediata in narrazione: `spinteResidue` è la vista d'insieme di cosa resta giocabile. Chip colorati per seme. Requisito di Saverio: avere sempre sott'occhio quali spinte restano mentre si sceglie la carta.
- **Esito di scena scritto da Claude quando vince l'Opposizione** (giugno 2026): in modalità solo, quando una scena si chiude a favore dell'Opposizione (`rec.vincitore==="O"`), l'esito non lo scrive più il giocatore ma Claude, in sola lettura. Vale per tutte le scene: per le scene 1-4 in `rFineScena` la textarea del riassunto è sostituita da uno slot in sola lettura con bottone disabilitato finché l'esito non arriva; per la scena 5 `conteggioDefinitivo` instrada alla schermata `fine_scena` (bottone "Vai al finale") invece di andare dritto al finale. La generazione è `iaEsitoScena()` (sul modello di `iaNarrazione`): chiede l'esito in 1-2 frasi, lo salva in `rec.riassunto`, lo registra nel log come voce dell'Opposizione (`aggiungiStoria("O", …, {tipo:"esito"})`, nuovo tipo con icona 🏁) e abilita il bottone; fallback se Claude non risponde. Quando vincono i Protagonisti o è parità, resta il riassunto scritto dall'umano. Tutto sotto guardia `modalità solo`: il transcript a 2 giocatori resta invariato. Requisito di Saverio.
- **Barra colorata dei segmenti del log vs Claude** (giugno 2026): nel log "Storia della partita" (modalità solo) ogni segmento ha una barra-titolo a larghezza piena con nome del giocatore (`nomeLato`), carta giocata (numero + icona seme, già in `v.carta`) e tipo di azione con icona (`mossaInfo`). Il colore della barra è **per lato**, non per seme: Protagonisti viola (`--viola #6a4c93`, testo bianco), Antagonisti grigio asfalto (`--pannello`, bordo, testo osso). Scartati i vecchi blu/rosso (`--latoP`/`--latoO`, rimossi) perché rimandavano ai semi cuori/quadri; il seme resta leggibile nell'icona della carta. Solo `renderStoria()` + CSS, nessuna mutazione di `G`. Requisito di Saverio. Transcript seed 13 invariato.
- **Overlay "Claude sta generando" vs Claude** (giugno 2026): in modalità solo un overlay a tutto schermo (`#veloIA`, modellato su `#velo`, `z-index:65` così copre anche `#iaBadge`) appare appena il turno passa all'Opposizione. La visibilità è derivata da `G` con il predicato `claudeStaGiocando()`, che rispecchia le condizioni di dispatch di `iaScheduler` (asta/apertura/turno/narrazione/mano_estesa/colpi e fine_scena con esito a favore di O), **escluso il mercato** (fase condivisa, non va coperta o bloccherebbe gli acquisti dell'umano). `render()` chiama `veloIA(claudeStaGiocando())` **prima** del dispatch di fase, così l'overlay copre i campi modificabili dell'apertura (`a-titolo`/`a-posta`) nello stesso frame in cui vengono dipinti: niente più flash dei campi prima che intervenga `iaApertura`. Nelle fasi che restano in attesa dell'umano (narrazione, esito) `iaNarrazione`/`iaEsitoScena` chiamano `veloIA(false)` dopo aver iniettato il testo in sola lettura e abilitato "Avanti" (anche nelle guardie di early-return), così l'overlay non copre lettura e avanzamento. UI transitoria fuori da `G` (coerente col requisito online: `G` si replica, l'overlay no). Contestualmente corretto un residuo della barra colorata: lo `<span class="carta">` della barra del log collideva con la `.carta` globale delle carte da gioco (rendendolo come una carta grande), classe rinominata in `crt`. Requisito di Saverio. Transcript seed 13 invariato.
- **Piatto visibile in narrazione** (giugno 2026): la schermata di narrazione (`rNarrazione`, sia il ramo dell'umano sia il ramo in sola lettura quando narra Claude) mostra ora il piatto corrente con il renderer mini (`G.piatto.map(c=>cartaHTML(c,"mini"))`, etichetta "Il piatto ora"), come già fa il turno. Così durante tutta la fase di gioco delle carte (turno + narrazione che ne segue) il piatto resta sempre visibile e il giocatore può valutare se vale la pena spendere una spinta del pitch per trasformare la presa in scopa. Pura lettura di `G.piatto`, nessuna mutazione. Il previsto accorciamento del testo regole (`n.sugg`) è stato lasciato perdere su scelta di Saverio: `adattaViewport()` riscala già la schermata. Transcript seed 13 invariato.
- **Log vs Claude a tab per scena** (giugno 2026): nel log "Storia della partita" (modalità solo) le scene non sono più impilate ma divise in tab, uno per scena. `renderStoria()` disegna una barra `#storiaTabs` (un tab per scena presente in `G.storia`, etichetta = numero; la scena corrente mostra numero + fase di `FASI_NARR`) e poi mostra **solo le voci della scena attiva**. Ogni tab passato porta un pallino del vincitore: viola (`--viola`) = Protagonisti, grigio (`--grigio`) = Opposizione/Claude, neutro (bordo) = parità o scena ancora in corso (letto da `G.scene[i].vincitore`, sola lettura). Lo stato del tab visualizzato è **UI pura, fuori da `G`** (variabili a livello di modulo `storiaTabAttiva`/`storiaScenaMax`, come `veloIA`/`iaBusy`: `G` si replica tra i client online, la scena che il giocatore sta guardando no). `aggiornaStoriaLog()` fa **auto-follow**: il tab attivo torna alla scena corrente alla prima apertura e a ogni nuova scena; se l'utente apre a mano un tab passato ci resta finché la partita non avanza di scena. Il click sui tab opera per indice di scena e re-renderizza solo il log, niente `render()` globale; bottone 🔑 e scroll a fondo (`vicinoFondo`) preservati. CSS a px fissi come tutto `#storiaLog`, **niente `var(--k)`**: la colonna `#storiaCol` è sorella di `main`, scorre da sé ed è fuori dal fit di `adattaViewport()` (che scala solo `main`). Solo `renderStoria`/`aggiornaStoriaLog` + CSS, nessuna mutazione di `G`. Requisito di Saverio. Transcript seed 13 invariato.
- **Interfaccia adattiva, niente scroll di pagina** (giugno 2026): tutte le dimensioni chiave (carte, mini, padding di main/pannelli/piatto, gap, banner, opzioni, titoli) sono espresse come `calc(base * var(--k,1))`. La funzione `adattaViewport()` riparte da `--k = 1` e scende a passi di 0.05 (fino a 0.5) finché `main.scrollHeight ≤ main.clientHeight`; è agganciata a un MutationObserver su `main`, al resize e a `document.fonts.ready`, quindi copre anche i pannelli che compaiono senza re-render (es. opzioni di presa). Solo visuale: non tocca mai lo stato `G`. I font hanno minimi di leggibilità via `max()`. Fallback se a scala minima non basta: scroll di `main` (resta possibile nelle schermate con form lunghi, es. wizard). Il piatto non ha più max-height fissa. Motivo: su Chrome desktop la schermata del turno superava l'altezza della finestra; requisito di Saverio: tutto adattivo, mai scroll durante il gioco.
- **Colore dedicato agli antagonisti nel log + quadri celeste** (giugno 2026): nel log "Storia della partita" gli Antagonisti non usano più il grigio asfalto (`--pannello`), che si confondeva con tutti gli sfondi scuri, ma un colore dedicato `--acciaio #2f6d8c` (teal, testo bianco), simmetrico al `--viola` dei Protagonisti; aggiornati barra-titolo (`.voce.o .voce-bar`), bordo del testo (`.voce.o .voce-testo`) e pallino del tab vincitore (`.tdot.o`, prima `--grigio`). Contestualmente il seme `--quadri` passa da `#4f7cac` a un celeste più chiaro `#6cb2e0`, per restare ben distinto dall'acciaio degli antagonisti e più leggibile sulle carte scure (vale anche per `.tot-lato.o` che usa `--quadri`). Solo palette CSS + commenti, nessuna mutazione di `G`, transcript seed 13 invariato. Requisito di Saverio.
- **Mano del turno in riquadro col colore del lato + spinte come conteggio nel recap** (giugno 2026): nella videata del turno (`rTurno`) la `.mano-area` ha ora un riquadro con sfondo a bassa opacità del colore del lato che gioca — viola per i Protagonisti (`.lato-p`, sfondo `rgba(106,76,147,.22)`), acciaio per gli Antagonisti (`.lato-o`, sfondo `rgba(47,109,140,.22)`), con la cornice esterna nel colore pieno del log (`var(--viola)` / `var(--acciaio)`) — per dare colpo d'occhio su di chi è il turno; la mano è di semi misti, quindi la tinta è del lato, non del seme. La stessa tinta del lato è applicata anche al pannello della mano nell'**asta** (`rAsta`) e al box **"La tua narrazione"** in modalità solo (`rNarrazione`, sempre lato P perché narra l'umano = Protagonisti); regole generiche `.pannello.lato-p`/`.lato-o` riusano i due colori. Inoltre le **spinte disponibili non sono più mostrate come chip estese con il testo della risposta** sopra la mano (rivede in parte la decisione "Spinte disponibili accanto alla mano"): ora compaiono come semplice conteggio per seme nel recap del titolo mano, accanto a prese/scope/mazzo (`spinte ♥ x · ♠ y`, simboli colorati per seme; per l'Opposizione ♦/♣). Conteggio da `spinteResidue(l)`, nascosto nell'ultima scena (`G.scena<4`) dove le spinte sono vietate. Il componente chip (`.spinta-chip`) resta in uso per "PNG creati". Solo `rTurno` + CSS, nessuna mutazione di `G`, transcript seed 13 invariato. Requisito di Saverio.
- **Service worker: la network-first salta la cache HTTP** (giugno 2026): `sw.js` dichiarava strategia network-first "per non servire versioni stantie", ma `fetch(e.request)` passava comunque per la cache HTTP del browser, così `index.html` poteva tornare stantio anche online (sintomo: modifiche al codice non visibili dopo l'aggiornamento). Corretto in `fetch(e.request, { cache: "reload" })`, che bypassa la cache HTTP e prende sempre il file fresco dal server (la cache `CACHE` resta solo per l'offline). Bumpata la versione cache `frenemies-v1` → `frenemies-v2` così il vecchio worker viene rimpiazzato e la vecchia cache purgata in `activate`. Ricordarsi di bumpare la versione a ogni release.

- **Casella tipo checkbox accanto alle spinte nelle Schede** (giugno 2026): nella scheda (overlay `apriSchede`, funzione `qa()`) ogni spinta ha ora una casella quadrata (`.spinta-check`) accanto alla risposta: vuota se la spinta è ancora disponibile, spuntata (✓, sfondo mezzeria) quando è già stata usata (`G.pitch[i].usata`, impostato da `eseguiSpinta`). Sostituisce il vecchio testo "· risorsa già spesa". La casella compare solo per le spinte vere, non per i due "detesta" narrativi (`PITCH_TEMPLATE[i].spinta===false`). Solo `qa()` + CSS, sola lettura di `G`. Requisito di Saverio.

- **Barra verticale + etichetta del lato di turno in ogni videata** (giugno 2026): `main` ha una barra verticale a sinistra (`border-left` 6px) colorata col lato che agisce — viola Protagonisti (`main.bar-p`), acciaio Antagonisti (`main.bar-o`) — e in alto a sinistra un'etichetta `#latoTurno` (pill col nome del lato, `nomeLato`, sfondo viola/acciaio, testo bianco). `render()` le aggiorna a ogni frame leggendo `latoCheAgisce()` (sola lettura di `G`), neutre nei menù e nel wizard di setup (`modalita`/`lobby`/`setup`), dove non c'è ancora un lato di turno. Coerente con i colori del log. Solo `render` + CSS + un nodo `#latoTurno` dentro `main`, nessuna mutazione di `G`. Requisito di Saverio.

- **Schede PNG con i colori del lato** (giugno 2026): nelle Schede → PNG, i pannelli dei PNG passano dai vecchi verde/rosso (`.png-prot`/`.png-opp`) ai colori del lato coerenti col resto: viola per i PNG dei Protagonisti (♥♠), acciaio per quelli dell'Opposizione (♦♣), sfondo `.22` e cornice piena `var(--viola)`/`var(--acciaio)`. Solo CSS. Requisito di Saverio.

- **Figure giocate con l'interfaccia del Jolly** (giugno 2026): `opzioniFigura` non mostra più la selezione libera delle carte del piatto (tocca-le-carte, somma ≤ valore). Ora, come il Jolly, la figura **dichiara un valore** tramite pulsanti (Fante 1–8, Regina 1–9, Re 1–10; i valori senza prese sono disabilitati) e poi sceglie tra le **prese esatte** di quella somma (`combosPresa(v)`, contenitore `#figCombos`). È un cambio di **sola interfaccia**: l'unione delle prese esatte per ogni v da 1 a max coincide con "tutti i sottoinsiemi di somma ≤ max", quindi l'insieme delle prese possibili e gli esiti non cambiano, e la regola KB §16 (cattura flessibile, somma ≤ valore) resta valida (nessuna modifica a KB, REGOLE, REGOLE_IA). Restano invariati i rami sacrificio (piatto vuoto) e "metti nel piatto" (nessuna carta alla portata). L'IA continua a usare `comboFiguraDefault` direttamente, fuori dalla UI. Driver di collaudo aggiornati: `test_partita.js` segue i `data-v`/`data-c` anche per le figure; `partita_esempio.js` riproduce la presa di default (espone `__comboFiguraDefault`/`__combosPresa`, sceglie il valore = somma del default e il combo corrispondente) così il **transcript seed 13 resta invariato**. Requisito di Saverio.

- **"🔁 Rigenera": rigenerazione dei contenuti di Claude col feedback del giocatore** (giugno 2026): in modalità solo il giocatore può rifiutare un contenuto generato da Claude e farlo rigenerare (pulsante "🔁 Rigenera"; gli identificatori interni restano `rifiutoRiprova`/`*Riprova`), indicando facoltativamente cosa non andava. `chiamaClaude(istruzioni, feedback)` accetta ora un secondo argomento: se `feedback != null` (anche stringa vuota) accoda al prompt utente una nota "il giocatore ha rifiutato, rigenera una versione diversa" + le parole del giocatore. Helper condivisi: `canaleIA()` (`claudeKey||inArtefatto()`), `rifiutoRiprova(onRigenera)` (modale con textarea facoltativa, `fbSi`/`fbNo`; tiene `iaBusy=true` per bloccare `iaScheduler` mentre l'umano decide e Claude rigenera). I prompt e l'iniezione sono stati estratti in funzioni riusate da prima generazione e rigenerazione, fonte unica: `istruzNarrBase`/`regSemeTxt`, `istruzApertura`, `istruzEsito`, `iniettaNarr`, `iniettaEsito`. La rigenerazione di narrazione ed esito rimuove prima la voce rifiutata dal log (`scartaUltimaStoriaO`, così non finisce nella continuità) e re-inietta nello stesso slot. Ambiti coperti: (1) **narrazione** dell'Opposizione (`#iaNarr`, pulsante `narrRiprova` accanto ad "Avanti"); (2) **esito di scena e finale** (`#fsEsito`, pulsante `fsRiprova`); (3) **apertura** (titolo/posta): con un canale reale `iaApertura` non avanza più in automatico ma si ferma a far rivedere i campi (modificabili) col pulsante `a-riprova`; senza canale resta l'auto-avanzamento di prima (fallback deterministico); (4) **setup Opposizione**: il vecchio "🃏 Reinventa" diventa "🔁 Rigenera" e passa per `rifiutoRiprova` → `generaOpposizioneIA(feedback)` (che ora rigenera anche se già fatta, quando arriva un feedback). Tutti i pulsanti compaiono solo con `canaleIA()`: senza chiave/artefatto non c'è nulla da rigenerare e `test_solo.js`/`partita_esempio.js` (senza canale) restano verdi, transcript seed 13 invariato. Niente tocca KB/REGOLE/REGOLE_IA: è un meccanismo di UI, non una regola di gioco. Requisito di Saverio.

## 5. Roadmap e stato

| Cosa | Stato |
|---|---|
| 2 giocatori pass-and-play, regole complete v1.1 | ✅ fatto e collaudato |
| 3/4 giocatori pass-and-play (controllo mano, scope per giocatore, ultima parola) | ✅ fatto e collaudato |
| 2 giocatori online (PeerJS) | ✅ implementato, **da testare dopo il deploy su Netlify** (la sandbox degli artefatti blocca la connessione) |
| 3/4 giocatori online | ⬜ non fatto — richiede topologia a più connessioni, ultimo step previsto |
| Deploy su Netlify + eventuale PWA | ⬜ da fare |
| Nomi dei PNG per le figure acquistate (scheda PNG della KB) | ⬜ non implementato |
| Pagamento manuale al mercato | ⬜ eventuale, se il playtest lo chiede |
| Modalità solo contro Claude (IA gioca l'Opposizione) | ✅ prototipo implementato e collaudato coi fallback; **da provare con chiamate reali nell'artefatto su Claude.ai**; per Netlify decidere chiave utente o proxy |
| Partita d'esempio per il manuale (driver a seed fisso + narrazione, 2 giocatori, scenario demo §33) | ✅ §34 riscritto sulle regole v1.4 (seed 13): scene PPPP+finale, Per il rotto della cuffia (piatto 33-0, punti 5-6), diario su-su-giù-giù-su. Si rigenera con `SEED=13 TRANSCRIPT=1 node test/partita_esempio.js` |

## 6. Collaudo

- `test_partita.js`: driver headless (jsdom) che gioca **partite complete** con scelte casuali, dalla creazione al finale, esercitando mercato, Jolly, spinte del pitch, mano estesa e colpi di scena. Le risposte del pitch vengono compilate quando i campi `w-pitch-*` sono presenti, non a un passo fisso del wizard (robusto a riordini dei passi).
- `test_adatta.js`: verifica l'adattatore di viewport in jsdom: con contenuto traboccante `--k` deve scendere finché tutto entra; con contenuto corto deve restare a 1.
- `test_solo.js`: collauda la **modalità solo** con uno stub di rete che risponde sempre JSON vuoto, così ogni decisione dell'IA passa dai fallback: la partita deve completarsi e le narrazioni IA devono comparire. Le chiamate vere a Claude si provano solo dentro l'artefatto in Claude.ai.
- `test_riprova.js` (giugno 2026): collauda il meccanismo **"🔁 Rigenera"**. A differenza di `test_solo`, inietta una **chiave fittizia** (così `canaleIA()` è vero e i pulsanti compaiono) e fa da stub con risposte **sempre diverse** (un contatore nel testo). Con seed fisso (mulberry32, default `SEED=7`) gioca una partita completa e, una volta per tipo, clicca il pulsante Rigenera (`w-reopp` al setup, `a-riprova` all'apertura di Claude, `narrRiprova` sulla narrazione, `fsRiprova` sull'esito), verificando che il **contenuto mostrato cambi** e che il **log narrativo non si duplichi** (lunghezza invariata: la voce rifiutata viene tolta e re-inserita). Fallisce se uno dei quattro casi non si verifica: il seed va scelto in modo che l'Opposizione vinca almeno un'asta (apertura) e almeno una scena (esito).
- Uso: `npm i jsdom && node test_partita.js` · variabile `NG=3` o `NG=4` per il numero di giocatori · adattare il percorso dell'HTML nella `readFileSync` se serve.
- Prassi: **una passata dopo ogni modifica al gioco**, a 2 e a 4 giocatori. È questo test che ha scoperto l'errore dei mazzi da 14 carte.
- Da giugno 2026 i driver `test_partita.js` e `partita_esempio.js` gestiscono anche i modali di acquisto figura (`pngOk`, campi `m-png-nome`/`m-png-desc`) e di creazione PNG narrativo (`pngnOk`, `m-pngn-nome`/`m-pngn-desc`): un acquisto al mercato avviene ora su due passi (click sul seme → modale nome/desc → conferma). `test_partita.js` esercita anche il pulsante "Crea PNG" una volta a partita; `partita_esempio.js` compila i modali in silenzio, senza alterare il transcript.
- `partita_esempio.js`: driver con **seed riproducibile** (RNG mulberry32 iniettato) e transcript completo, usato per generare la partita d'esempio del §34 (seed 13, pitch demo precompilato). Uso: `SEED=13 TRANSCRIPT=1 node partita_esempio.js`. Se una regola cambia, la partita del manuale si rigenera e si riverifica da qui.
- **Secondo bug scoperto dal collaudo automatico** (giugno 2026): l'ordine pesca/Jolly. `avviaMercato()` pescava la mano della scena 4 prima che il Jolly entrasse nei mazzi (l'inserimento avveniva solo alla chiusura del mercato), quindi il Jolly non era mai pescabile in scena 4, contro la KB §22. Corretto: il Jolly entra nei mazzi a inizio mercato, prima della pesca; la schermata di spiegazione resta a fine mercato (flag `jollyIntroVisto`). Il sintomo: nessun counter in 480 partite simulate.
- `test/test_ritirata.js` (giugno 2026): collauda la **resa**. Una partita per processo, forza la mossa appena compare il bottone (`#btnRitirata`/`#btnResa`), conferma nel modale (`#ritConferma`) e verifica: offerta solo a chi non ha l'iniziativa, con 1 carta, scene 1-4; `rec.perRitirata` = chi si arrende, vincitore = l'altro lato, flag variante settato, riassunto non vuoto, variante non più ripetibile, mazzo +1 col rimescolo della ritirata strategica. Ripetuto in loop copre entrambe le varianti.
- **Simulazioni di conferma v1.6** (giugno 2026): l'harness `sim.js`/`runner.js` (fuori dal repo) fa passare le figure per la vera `opzioniFigura` (niente più bypass) con una policy di resa razionale; 960 partite (480 a 2g, 480 a 4g) senza crash confermano i costi 3/5/8 e misurano la frequenza della resa.

## 7. Documentazione

- `frenemies_on_the_road_kb_v1_2.md` è la **fonte di verità** del regolamento (contenuto v1.6; il nome del file resta `..._v1_2`, il contenuto si autodichiara la versione). Il PDF si rigenera da lì (markdown → HTML → WeasyPrint) e riceve in coda le 5 pagine della scheda di partita dal PDF originale (pypdf merge). Aggiornare sempre prima il Markdown, poi rigenerare.
- Ogni nuova regola va annotata anche nel **Registro modifiche** in fondo alla KB e, se rilevante per il gioco digitale, nelle sezioni del regolamento in-game (oggetto `REGOLE` nel codice).
- `frenemies_manuale.md` (giugno 2026) è il **manuale del giocatore**: una riscrittura discorsiva e didattica della KB, pensata per la revisione di un editor (non per la stampa). È un documento **derivato**, non una fonte di verità: la KB resta l'unica fonte delle regole. Quando le regole cambiano, va aggiornata prima la KB (con il suo protocollo) e poi, separatamente, riallineato a mano il manuale. Scelte editoriali: flusso "impara giocando" sul gioco a 2; il gioco a 3/4 è solo accennato all'inizio e spiegato in appendice; rimossi apparato interno della KB (riferimenti §, tag di versione, registro modifiche, note rivolte all'IA); scenario demo, partita d'esempio (fatti meccanici del seed 13 invariati) e liste di ispirazione spostati in appendice. Non agganciato a `genera_pdf.py`, che continua a generare il PDF dalla sola KB.
- **Appendice B del manuale, formato "actual play" passo per passo** (giugno 2026): la partita d'esempio (scenario "Il sole di mezzanotte", seed 23 per le scene 1-2) è presentata passo per passo. Schema fisso: ogni passo ha prima una **riga d'azione** in testo normale che spiega cosa succede in gioco (chi cala quale carta e l'effetto: presa, scopa, minaccia, ecc.), e sotto un **riquadro** che contiene SOLO la fiction (la narrazione), con una didascalia "Paola narra" / "Omar narra". I riquadri sono di un **unico colore** (parchment, bordo oro): chi narra è indicato dalla didascalia, non dal colore. L'esito dell'asta e il conteggio finale restano in testo normale, fuori dai box. La scena si apre con due passi: "stabilisce il titolo e la posta" (box con titolo della scena + posta) lo fa il vincitore d'asta, e "fa il framing iniziale" (box con la descrizione iniziale) lo fa il giocatore SENZA iniziativa (così dalla regola v1.18; nella prima stesura erano entrambi del vincitore d'asta). Classi CSS in `genera_pdf_manuale.py`: `.azione`, `.fiction` (mono), `.fiction .chi`, `.fiction .titolo-scena`; nel markdown i riquadri sono `<div>` raw e le righe d'azione usano `attr_list` (`{: .azione }`). I titoli di scena nel manuale sono solo la fase ("Scena 1. Innesco"): il titolo narrativo ("L'asta del Cairo") è dichiarato in-gioco dal vincitore d'asta, nel box. Le righe d'azione portano anche la **motivazione strategica** di ogni giocata (perché quella carta dal punto di vista del gioco e come la meccanica/il seme suggerisce quella fiction), recuperata dai prompt originali con cui fu costruita la fiction: è il valore didattico dell'esempio e va preservato. Solo presentazione: carte, prese ed esiti restano quelli del seed 23. L'Appendice B si apre con un riassunto del pitch ("Il pitch in breve") e della prima difficoltà dello scenario "Il sole di mezzanotte", perché la partita usa quel pitch e non quello dell'Appendice A ("La strada della vendetta"): senza il recap le mosse non sarebbero comprensibili. NB: resta da sanare l'incoerenza per cui l'Appendice A descrive ancora lo scenario Frank/Skunk mentre la partita d'esempio usa Vera/Otto (conversione v1.12 non ancora completata).
- **Scena 4 dell'actual play scriptata a mano** (giugno 2026): a differenza delle altre scene (linea di gioco seed-derivata dalla policy del driver), la Scena 4 della partita d'esempio ha la successione delle giocate **scelta da Saverio** per farne una vittoria totale dell'Opposizione (3 scope). Implementata in `test/partita_esempio.js` con `S4_ORDINE`/`s4i` (override del solo turno quando `G().scena===3`) e `S4_SPINTA_TXT="Aldo"` per spuntare, alla mossa 6, la spinta ♣ del segreto («Aldo Falco è vivo: è un Vegliante. Otto lo ha capito, e tace») invece di quella del deserto. Verificata col solver minimax `scratchpad/solver4.js` (con quelle carte la scopa di Omar è inevitabile e 3 scope è l'ottimo per l'Opposizione). Le carte del mazzo non cambiano (l'ordine di gioco non tocca la pesca), ma lo scripting sposta il consumo dell'RNG: di conseguenza **la Scena 5 è cambiata** rispetto al vecchio scheletro (Re giocato per ultimo, Jolly come 4, finale 21‑0 nel piatto con Opposizione 20‑0 ai punti, sempre "per il rotto della cuffia"). Resta tutto riproducibile e protetto dal diff su `test/transcript_seed_23_43_5_v2.txt`. Inoltre, **posta** della Scena 4 rivista (il "cosa": "Trovare l'accesso alla cripta…", non più "prima dell'alba"; principio: le poste non sono mai indispensabili, cfr. immagine "Indiana Jones legato come un salame") e **titolo** "Nella città perduta" (da "L'accampamento"), allineati in playset json/scheda/index.html/driver/transcript. Fiction di Scene 4 e 5 ancora da scrivere su questa nuova base.
- **Manuale: ridotti i diagrammi di flusso a due (scena e partita)** (giugno 2026): rimossi i flowchart di dettaglio `asta` (§3.1) e `turno` (§3.7); la struttura della scena è ora coperta dal solo diagramma `flusso-scena`, posto in apertura del Capitolo 3 (subito dopo il cappello introduttivo, prima del §3.1: messo prima del cappello lasciava il titolo del capitolo da solo su una pagina, perché il diagramma grande prende una pagina orizzontale dedicata). I segnaposto sono stati tolti dal markdown e le chiavi `asta`/`turno` rimosse dal dict `DIAGRAMMI` in `genera_pdf_manuale.py` (le funzioni `diag_asta`/`diag_turno` restano definite ma inutilizzate, riusabili se servisse). Diagrammi di flusso rimasti: `flusso-scena` (Cap. 3) e `flusso-partita` (Cap. 1). Entrambi resi su **pagina verticale** (A4 portrait) invece che orizzontale: aggiunto a `figura()` il parametro `verticale=True`, con la classe CSS `.diag-pagina-v` (pagina ritratto dedicata, page-break prima/dopo, contenuto centrato) e le costanti `PORT_W_MM`/`PORT_H_MM` (area utile 174×235mm) per lo scaling. La pagina orizzontale (`.diag-pagina`, `@page paesaggio`) resta disponibile per gli altri diagrammi.
- **Manuale: diagramma "flusso della partita" spostato nel Capitolo 1** (giugno 2026): aggiunta la sezione **§1.10 "Flusso della partita"** in chiusura del Capitolo 1 (panoramica), con il diagramma `flusso-partita` e una spiegazione sintetica fase per fase (Pitch, Scene 1-4, Mercato, Ultima scena, Colpi di scena, Lettura del finale e domande, Epilogo). Il segnaposto `<!-- DIAGRAMMA: flusso-partita -->` è stato **spostato** (non duplicato: il generatore sostituisce tutte le occorrenze) dal §8.3, che ora rimanda al diagramma del Cap. 1 e tiene la versione discorsiva passo per passo. Voce d'indice del Cap. 1 aggiornata.
- **Manuale: riorganizzazione del Capitolo 3 ("Come si gioca una scena")** (giugno 2026): rivista in più passate la struttura del capitolo. Apertura: **§3.1 La struttura della scena** (spostata in testa, dall'ex §3.4) seguita subito dal **diagramma flusso-scena**, poi **§3.2 I rudimenti della scopa** (nuovo, hub delle basi) e **§3.3 Mano e asta** (ex §3.1); il resto slittato. L'ex paragrafo unico sulle tre mosse è **diviso in tre sezioni**: **§3.7 Introdurre** (con il sotto-blocco "i Qualcuno"), **§3.8 Prendere** (con il sotto-blocco "### Presa maldestra", esempio Vera/Otto), **§3.9 Fare scopa**. Le ex sezioni **§3.10 "Le regole di presa"** e **§3.10 "Segnare prese e scope"** sono state **accorpate in §3.2** per togliere i doppioni col primer. **§3.13 Arrendersi è stato spostato subito dopo §3.9 Fare scopa** (diventa §3.10: la resa è l'alternativa a giocare l'ultima carta, quindi sta tra le mosse e la fine del round), e in **§3.11 La fine del round** è specificato che il conteggio del piatto avviene "se l'ultimo a giocare non ha concesso la scena (vedi §3.10)". Stato finale del capitolo: §3.1 La struttura · §3.2 I rudimenti della scopa · §3.3 Mano e asta · §3.4 La posta · §3.5 L'apertura · §3.6 Che cosa puoi fare · §3.7 Introdurre · §3.8 Prendere · §3.9 Fare scopa · §3.10 Arrendersi · §3.11 La fine del round · §3.12 Narrare l'esito · §3.13 Che cosa succede dopo · §3.14 La continuità. **§3.2** contiene, oltre alle basi (piatto, presa per pari valore o per somma, presa obbligatoria con la sfumatura "obbligo per singola carta", scopa = piatto svuotato), tre sotto-sezioni: **"### Accortezze tattiche"** (carte alte per l'ultima giocata; non lasciare nel piatto un valore <10 spazzabile con una scopa; ricordare le carte uscite, l'avversario ha 2 copie per numero), **"### Differenze rispetto alla scopa tradizionale"** (punteggi presa 1/scopa 3; figure e Jolly "elastici"; prese promosse a scopa con una spinta del pitch) e **"### Segnare i punti"** (carte catturate → scarti comuni; carta della presa in verticale, della scopa in orizzontale; valgono al mercato e nel conteggio finale). Rinumerati i rimandi interni (posta §3.4, esito §3.12, arrendersi §3.10). Solo manuale, nessuna modifica a KB/regole (le fonti formali restano KB §16 prese e §17/§20 punteggio). NB: i due capoversi introduttivi del capitolo restano; il secondo ("In sintesi…") è ora in parte ridondante con §3.1, da valutare se accorparlo.- **Manuale: esempi in riquadro, esempio §3.4 convertito, diagramma "esiti" rimosso** (giugno 2026): (1) tutti gli esempi del manuale (18) sono ora impaginati in un **riquadro con fondino** (classe CSS `.esempio` in `genera_pdf_manuale.py`: sfondo sabbia, bordo sottile, font più piccolo; nel markdown si taggano con `{: .esempio }`, gli esempi incorporati a fine paragrafo sono stati separati in un loro capoverso). Dagli esempi nel box è stato tolto l'attacco "Per esempio:"/"Ad esempio,"/"Un esempio:" (ridondante, il riquadro li segnala già); resta solo nel crescendo §3.4, che non è boxato (è seguito da un elenco). (2) L'esempio di posta in §3.4, ancora su Frank/Skunk, è stato riscritto sullo scenario "Il sole di mezzanotte" (taccuino di Aldo all'asta del Cairo). (3) Rimosso il **diagramma "esiti"** (ex pag. 32 del finale), ridondante con la tabella dei quattro esiti in §7.2: tolto il segnaposto e la chiave `esiti` dal dict `DIAGRAMMI` (la funzione `diag_esiti` resta definita ma inutilizzata). **Sweep Frank/Skunk del manuale completato** (giugno 2026): convertiti a Vera/Otto / "Il sole di mezzanotte" anche gli ultimi esempi rimasti (§3.9 Fare scopa, §3.14 continuità, §5 figure Fante/Regina/Re, §6 Jolly sleale e marcatore, §7 colpi di scena) e uniformato "Serge il Concierge" → "Hatim" in §3.7. Nel manuale non restano più occorrenze di Frank/Skunk. (Restano invece da bonificare gli esempi inline della **KB** e i blocchi `.es`/esempi di tono di `REGOLE`/`REGOLE_IA` in `index.html`, fuori da questo passaggio.)
- **Manuale: eliminato il Capitolo 8 "Come giocare bene"** (giugno 2026): giudicato ridondante. §8.2 (tabella delle tre mosse) e §8.3 (flusso passo per passo) erano doppioni di §1.7/§3.7-3.9 e di §1.10; l'unico contenuto non presente altrove era §8.1 "Due strategie nell'ultima scena" (controllo vs pressione), che è stato eliminato con il resto (recuperabile, se servisse, spostandolo in Cap. 6 L'ultima scena). Tolti anche la voce d'indice e il diagramma `tre-gesti` (segnaposto + chiave nel dict `DIAGRAMMI`; funzione `diag_tre_gesti` lasciata definita ma inutilizzata). Il manuale ora ha 7 capitoli + appendici A-E; nessun rimando esterno puntava al Cap. 8.
- **Manuale: §1.10 "Flusso della partita" tutto su una pagina** (giugno 2026): il diagramma `flusso-partita` ora è reso **inline** (non più su pagina orizzontale/verticale dedicata) tramite il nuovo parametro `figura(larghezza_mm=...)` (135mm, centrato); il titolo §1.10 ha `{: .salto-pagina }` per iniziare a pagina nuova, e l'elenco "fase per fase" è stato accorciato a una riga per voce, così titolo + intro + diagramma + elenco stanno tutti sulla stessa pagina.
- **Manuale: §2.8 "Come si usano le spinte" spostato in Capitolo 3 e rinominato "Darsi una spinta"** (giugno 2026): scelta di Saverio. La sezione sull'uso in gioco delle spinte (che il pitch definisce) è uscita dal Capitolo 2 (che ora finisce a §2.7) ed è collocata in Capitolo 3 **dopo §3.9 Fare scopa**, come **§3.10 "Darsi una spinta"** (la promozione presa→scopa si capisce meglio dopo aver spiegato presa e scopa). Stato finale del Cap. 3: §3.1 La struttura · §3.2 I rudimenti della scopa · §3.3 Mano e asta · §3.4 La posta · §3.5 L'apertura · §3.6 Che cosa puoi fare · §3.7 Introdurre · §3.8 Prendere · §3.9 Fare scopa · §3.10 Darsi una spinta · §3.11 Arrendersi · §3.12 La fine del round · §3.13 Narrare l'esito · §3.14 Che cosa succede dopo · §3.15 La continuità. Rimandi aggiornati: prese con spinta (§3.2) → §3.10, posta → §3.4, arrendersi → §3.11, narrare l'esito → §3.13.
- **Terminologia v1.24: il secondo piano del finale è la «Crescita», non il «prestigio»** (giugno 2026): scelta di Saverio, che a «prestigio» preferisce **Crescita** (già nome del piano nel manuale del giocatore); «prestigio» resta solo come una delle interpretazioni possibili dentro la definizione. Aggiunto un paragrafo di **definizione** al finale: i punti non spesi misurano quanto i personaggi di un lato sono cresciuti lungo il viaggio (pochi punti = crescita modesta); il significato è lasciato all'interpretazione del giocatore (maturazione, evoluzione interiore, oppure reputazione e prestigio); e il **seme** delle carte con cui si sono fatti i punti dà un indizio di che cosa sia cresciuto di più, confrontando i **due semi del proprio lato** (per i Protagonisti Cuori = legame/empatia vs Picche = forza/affermazione; per l'Opposizione Quadri = nemici vs Fiori = caos). Aggiornata la domanda di fine partita §26.1 («sono cresciuti o hanno accresciuto il loro prestigio?» → «sono cresciuti?»). Propagato: KB §26/§26.1 (+registro v1.24 e footer), manuale con definizione **sintetica** in §1.9 («Come finisce?») e definizione **estesa** (lettura per seme) nella nuova sezione **§7.2 «Crescita»** — gli esiti scalano a §7.3, le domande a §7.4 (dov'è la domanda aggiornata), l'epilogo a §7.5 — più `REGOLE` e `REGOLE_IA` in `index.html`. Il motore non usa la parola, nessun effetto sul codice. **Bonifiche nello stesso commit:** residuo «Obiettivo finale:» → «La Missione:» nello scenario demo KB §33 (leftover v1.19) e refuso «spende il del Jolly» → «spende il marcatore del Jolly» nel manuale §5.3. NB: il diario non ha mai avuto una voce dedicata per la **v1.22** (parità dei totali → posta a chi gioca l'ultima carta); il registro del KB invece sì.
- **Manuale: sezione «Crescita» dedicata, glossario e impaginazione** (giugno 2026, contestuale alla v1.24): (a) la definizione estesa della Crescita è diventata una sezione a sé, **§7.2 «Crescita»** (esiti → §7.3, domande → §7.4, epilogo → §7.5), mentre §1.9 tiene solo la definizione sintetica; (b) in §7.4 aggiunto un capoverso che lega le domande di fine partita all'epilogo (le risposte mettono a fuoco la storia e fissano i fatti usati nelle vignette); (c) nuova **Appendice F. Glossario**, tutti i termini di gioco in ordine alfabetico con definizioni allineate al KB v1.24; (d) corretto il footer del manuale, fermo a «Knowledge Base v1.6» → **v1.24**; (e) paginazione PDF: aggiunto `break-inside: avoid` ai paragrafi in `genera_pdf_manuale.py` e un blocco analogo in `genera_pdf.py`, per non spezzare i paragrafi su due pagine. Solo manuale/PDF, nessun effetto su regole o motore; il glossario è un ausilio del solo manuale, non propagato a KB/`index.html`.
- **Regola v1.23: una figura non resta mai nel piatto, si sacrifica** (giugno 2026): segnalazione di Saverio, nata togliendo la §23.2 dal manuale. Prima una figura senza bersaglio (piatto con sole carte di valore superiore al suo) *entrava nel piatto* e ci restava; ora **non resta mai sul tavolo**: si **sacrifica** e va tra le prese di chi la gioca. È una **scopa** se il piatto era vuoto (gesto eroico, caso già v1.6), altrimenti una **presa** a vuoto da 1 punto, lasciando nel piatto le carte fuori portata. Le figure diventano puri strumenti di cattura, non "muri" da lasciare sul tavolo. Conseguenza: la **§23.2** (figure rimaste nel piatto a inizio quinta scena → rimosse e contate come scopa per l'avversario) perde ogni caso d'uso ed è **rimossa** ovunque. Propagato: KB §15.3/§16 (+registro v1.23 e footer), §23.2 e diagramma §29 tolti, riferimento "alleati sacrificati" tolto dal tie-break §25; manuale §3 (cattura figure, distinzione vuoto=scopa/fuori portata=presa) e Capitolo 6 (§6.2 eliminato, numerazione richiusa 6.3→6.2, 6.4→6.3, tie-break §6.3 ripulito); `index.html` motore (fase `neutralizza`, `rNeutralizza`, stato `neutralizzati` e routing eliminati; in `giocaCarta`/`opzioniFigura`/generatore mosse IA nuovo esito `sacrificio_presa`), `REGOLE` (box §23.2 tolto, regola figure aggiornata) e `REGOLE_IA` (più correzione di un residuo "presa con figura vale 2" → 1). Test verdi (2g/4g, adatta, solo, riprova) e transcript d'esempio invariato.
- **Chiarimento v1.21: la carta d'asta del vincitore resta scoperta** (giugno 2026): segnalazione di Saverio. La carta con cui si vince l'iniziativa, già rivelata nell'asta, si tiene **a faccia in su** sul tavolo fino a quando il vincitore la rigioca come ultima carta del round. È un dato già pubblico (le due carte d'asta si rivelano insieme), ma renderlo esplicito porta un punto strategico: l'avversario sa fin dall'inizio con quale carta il vincitore chiuderà e pianifica di conseguenza, contrappeso al vantaggio dell'iniziativa. Aggiunto alla nota «l'iniziativa conviene sempre?» (manuale §3.1). Propagato a KB §11/§13 (+registro v1.21 e footer), manuale §3.1, `REGOLE` e `REGOLE_IA` in `index.html`. Solo chiarimento, nessun effetto sul motore.
- **Regola v1.20: il framing entra nella prima giocata (supera la v1.18)** (giugno 2026): ripensamento con Saverio sulla v1.18. Un passo di "framing" separato, prima di calare una carta, era l'unico momento di narrazione senza carta (contro il principio "si narra l'effetto della mossa") e spezzava il ritmo. Ora **non c'è più un passo di framing**: chi vince l'asta fissa titolo+posta e poi **apre la scena giocando la prima carta**, la cui narrazione mette in quadro insieme situazione/atmosfera iniziale ed effetto della carta (vedi esempio: «Siamo in un vicolo buio… è Hatim che apre la porta sul retro»). Conseguenze: il framing torna a chi ha l'iniziativa (gioca per primo), con la simmetria "chi vince l'asta apre / chi perde la posta chiude"; l'atmosfera (§12.1) è suggerita dal seme della **prima carta giocata**, non più della carta d'asta. Propagato: KB §12/§12.1 (+registro v1.20 e footer), manuale §3.1/§3.3/§8.3, `REGOLE`/`REGOLE_IA` in `index.html`, **diagramma flusso-scena** (tolto il box "L'altro fa il framing"; la prima giocata apre la scena), e **Appendice B** (i 4 box di framing rifusi nella prima giocata del vincitore: S1 Paola/5♥, S2 Omar/7♦, S3 Omar/6♦, S4 Paola/7♠) + file di appoggio. Regola sociale al tavolo, nessun effetto sul motore. **Codice app ancora da rivedere** (opzionale): in vs Claude il vincitore continua a fissare solo titolo+posta (corretto); il suggerimento `AMBIENTE[seme]` è ancora legato alla carta d'asta e andrebbe semmai spostato sulla prima giocata, ma è solo un hint UI.
- **Terminologia v1.19: «missione» al posto di «obiettivo finale»** (giugno 2026): scelta di Saverio di usare un solo termine per il traguardo della partita. «obiettivo finale» (e i bare «obiettivo» che indicavano il traguardo) → **missione**, già nome interno nel motore (`G.missione`/`w-missione`) e nome del piano nel finale. Stile: **Missione** maiuscola in titoli/etichette/elenco del pitch (come «L'Opposizione»), **missione** minuscola nella prosa (come «posta»). Tenuti «obiettivo intermedio»/«obiettivo concreto» per la posta di scena. Adattati i verbi (raggiungere l'obiettivo → compiere la missione) e riscritta la frase del finale per togliere la circolarità («la missione, cioè se i protagonisti l'hanno compiuta»). Identificatori JS non toccati. Propagato a KB (+registro v1.19 e footer), manuale e `REGOLE`/`REGOLE_IA`/etichette in `index.html`. Le voci storiche del registro KB restano invariate (le 3 occorrenze di «obiettivo finale» lì sono volutamente lasciate).
- **Regola v1.18: il framing iniziale lo fa il giocatore senza iniziativa** (giugno 2026): l'apertura della scena ora si divide in due. Chi vince l'asta sceglie **titolo e posta** (la cornice); poi è **l'altro giocatore**, quello senza iniziativa, a **fare il framing iniziale**, mettendo in scena la situazione e l'atmosfera (prima le imponeva lo stesso vincitore dell'asta). La tabella **seme→atmosfera** (KB §12.1) **resta** e la usa chi fa il framing, seguendo il seme della carta con cui l'avversario ha vinto l'asta (scelta di Saverio tra le opzioni "resta/la usa l'altro" vs "ispirazione generica" vs "framing libero"). Nata dai due flowchart che Saverio ha disegnato in `docs/flussi.pdf`. Propagata: KB §12 + §12.1 + registro v1.18 + footer; manuale §3.3 e §8.3; `REGOLE` ("Apertura della scena") e `REGOLE_IA` ("ASTA E APERTURA") in `index.html`. Regola sociale al tavolo: nessuna modifica al motore (in vs Claude è riflessa nel testo di `REGOLE_IA`, così che quando vince l'asta l'umano apra la scena e quando vince l'umano sia Claude a fare il framing). Resa coerente anche tutta la fiction: nell'actual play dell'Appendice B i quattro box "fa il framing iniziale" sono stati riassegnati al giocatore senza iniziativa (S1 Omar, S2 Paola, S3 Paola, S4 Omar; didascalia del box scambiata, descrizione invariata), insieme alla nota strategica §3.1, all'intro della Scena 1 e ai file di appoggio `appunti_partita_esempio.md` e `fiction_esempio_scene1-2.md`. Aggiornati di conseguenza i due **diagrammi di flusso del manuale** (`diag_flusso_scena` e `diag_flusso_partita` in `genera_pdf_manuale.py`) ridisegnati secondo `flussi.pdf`: la scena ora mostra pesca→asta→(vincitore: posta+titolo)→(l'altro: framing)→4 turni→bivio resa→esito narrato da chi perde la posta; la partita mostra pitch→scene 1-4 col bivio mercato→ultima scena→colpi di scena→lettura finale e domande→epilogo a vignette (loop "hanno passato entrambi")→fine.
- **Regola v1.17: la prima difficoltà la decide l'Opposizione** (giugno 2026): la risposta condivisa che inquadra la Scena 1 (KB §9.7) prima «apparteneva a tutto il tavolo» senza un arbitro in caso di disaccordo. Ora si decide insieme ma **l'ultima parola spetta all'Opposizione** (scelta di Saverio, nata riscrivendo il manuale §2.7). Resta vero che chi vince l'iniziativa della Scena 1 la usa per inquadrarla. Propagata: KB §9.7 + registro v1.17 + footer; manuale §2.7 (già riscritto a mano); `REGOLE` (scheda "Risorse del pitch") e prompt del wizard di setup (passo 8) in `index.html`. `REGOLE_IA` e `setupPerIA`/`statoPerIA` non enunciano la regola (passano solo il valore di `primaDiff`), quindi nessuna modifica lì.
- **Manuale: esempi del Capitolo 2 (pitch) convertiti allo scenario "Il sole di mezzanotte"** (giugno 2026): i cinque esempi del capitolo del pitch (coppia 2.2, obiettivo 2.3, opposizione 2.4, prima difficoltà 2.7, spinta spuntata 2.8) erano ancora sul vecchio scenario Frank/Skunk/Vargas ("La strada della vendetta"). Riscritti sul playset Vera Falco (♠) / Otto Lenzi (♥), Loggia del Basilisco + Veglianti (♦), il deserto (♣), in coerenza con le Appendici A e B. (Saverio ha poi riscritto a mano i Capitoli 1 e 2: tra l'altro 1.2 con tabella di film di riferimento, 2.1 "genere e tono", e ordine sezioni invertito, obiettivo 2.2 prima della coppia 2.3.) La spinta d'esempio in 2.8 è ora quella ♣ del segreto su Aldo Falco ("è vivo, è diventato un Vegliante; Otto lo ha capito e tace"), la stessa che la partita d'esempio spunta nella Crisi. La nuova sezione 1.6 sui semi è stata lasciata da Saverio senza esempi inline (solo glosse), quindi neutra. **Resta da convertire** lo sweep degli esempi inline Frank/Skunk ancora presenti nel Cap. 3 (poste/scope) e nei Cap. 4-5 (figure, Jolly).
- **Manuale: Appendice G "Le schede da fotocopiare"** (giugno 2026): aggiunte in fondo al manuale (dopo il Glossario, prima del colophon) tre schede cartacee A4 da stampare e compilare a penna al tavolo, versione offline di ciò che il wizard di setup digitale raccoglie: (1) **scheda del protagonista**, una pagina per ciascuno dei due eroi (♠ Picche e ♥ Cuori, su pagine separate per fotocopiarne una a testa): nome, interpretato da, chi è, cosa detesta dell'altro (spunto narrativo) e le due spinte del seme (motivazione + asso nella manica) con casella "usata"; (2) **foglio del pitch**, il quadro condiviso: protagonisti, tono, premessa, missione, prima difficoltà, l'Opposizione (♦ nemici / ♣ caos) e le quattro spinte ♦♣ con casella "usata" (le altre quattro ♠♥ stanno sulle schede dei protagonisti); (3) **carte dei Qualcuno**, sei mini-card 2×3 da ritagliare (nome, volontà, carta che lo rappresenta, tipo PNG/Figura/Gruppo/Luogo, casella "catturato"). Implementazione: contenuto come HTML grezzo dentro `frenemies_manuale.md` (niente blank-line dentro i blocchi `<div>`, altrimenti Markdown li ri-processa) + classi CSS con prefisso `sg-` aggiunte alla stringa `CSS` di `genera_pdf_manuale.py` (palette del manuale, layout flexbox; `.sg-break` per il page-break tra i fogli). NB: il testo delle domande del pitch e i termini di gioco sono **copiati a mano** dalla fonte (PITCH_TEMPLATE in `index.html` / KB); se cambiano le domande del pitch o la terminologia, questa appendice va riallineata come le altre copie del manuale. Solo manuale, nessun effetto su KB/regole/motore.
- **Manuale: il vecchio Capitolo 3 "I quattro semi" diventa la sezione 1.6** (giugno 2026): un intero capitolo dedicato al significato dei semi era sovradimensionato (e in parte già anticipato da 1.5 "I materiali"). Condensato in una nuova sezione **1.6 "Che cosa significano i semi"** del Capitolo 1 (lead + elenco a quattro voci con un esempio inline per seme, al posto delle quattro sottosezioni 3.1-3.4). Di conseguenza i capitoli 4-9 scalano a **3-8** e tutte le loro sottosezioni e i riferimenti incrociati sono stati rinumerati (indice compreso). Nell'occasione sono stati corretti alcuni rimandi già errati nel corpo (puntavano a numeri inesistenti): spinte ora "vedi 2.8", resa "vedi 3.12", nomi figure "vedi 5.3", primo conteggio ultima scena "vedi 3.10". Solo manuale (`frenemies_manuale.md`): la KB non ha un capitolo equivalente. Il PDF si rigenera con `genera_pdf_manuale.py`.
- **Terminologia: eliminata la locuzione "scopa vera"** (luglio 2026): scelta di Saverio. Non esiste il concetto di "scopa vera" né di "scopa finta": c'è solo la **scopa**. Rimossa ogni occorrenza di «scopa vera» sostituendola con «scopa» in KB (§9.8 testo regola + nota v1.2 + registro §9.8), manuale (§3.10 spinte, box esempio, appendice B), `index.html` (commento del motore, `REGOLE` uso meccanico 9.8) e file di appoggio (`fiction_esempio_scene1-2.md`), oltre alle voci storiche di questo diario (punti 6 e 18 della prima sezione). Restano solo i riferimenti storici alla «finta scopa» della v1.1 come nome di una regola superata, non come concetto attivo. Solo terminologia, nessun effetto sul motore.
- **UI: il flusso della partita va a tutta larghezza** (luglio 2026): scelta di Saverio. Rimosso il tetto `max-width:980px` (con `margin:0 auto`) su `#scena-corrente` in `index.html`, che teneva la partita in una colonna stretta centrata; ora il contenitore usa `max-width:none;width:100%` e la scena occupa tutta la larghezza disponibile di `<main>` (tavolo, mani e mercato restano centrati al loro interno via `justify-content:center`). Solo layout: lo stato `G` e `adattaViewport()` non sono toccati; il fit verticale continua a funzionare (più larghezza => meno wrapping => `--k` eventualmente più alto). Collaudo: `test_partita.js` (NG=2 e NG=4) e `test_adatta.js` verdi.
- **Manuale: le due schede dei protagonisti sulla stessa facciata, via "Interpretato da"** (luglio 2026): scelta di Saverio. Nell'Appendice G rimosso il campo "Interpretato da" da entrambe le schede protagonista (♠ Picche e ♥ Cuori) e tolto il `.sg-break` che le teneva su pagine separate: ora stanno insieme su una sola facciata (una fotocopia serve entrambi gli eroi). Per farcele stare, in `genera_pdf_manuale.py` le classi `sg-` sono state compattate (titolo 18→15pt, occhiello 10→9.4pt, margini/padding di `.sg-lab`/`.sg-box`, altezza righe 19→16px, spinte più fitte) e aggiunto `.sg-foglio { break-inside: avoid }` perché nessuna scheda si spezzi a metà; nelle due schede ridotte le righe di "Chi è" (4→3), "Cosa detesta" (3→2) e le spinte a una sola riga di scrittura (come quelle ♦♣ del foglio del pitch). Il foglio del pitch e le carte dei Qualcuno restano su facciate proprie. Solo manuale: nessun effetto su KB/regole/motore. PDF rigenerato.
- **Manuale: "foglio del pitch" scisso in "Scheda del viaggio" + "Scheda dell'opposizione"** (luglio 2026): scelta di Saverio. Nell'Appendice G l'unico foglio del pitch è diventato due schede su facciate separate. (1) **Scheda del viaggio** (ex "Il pitch · foglio del tavolo", rinominata): tolto il box "I protagonisti" (i nomi stanno già sulle schede dei protagonisti), restano Tono e Genere; la scheda arriva fino a "La prima difficoltà" e i tre box di testo (premessa, missione, prima difficoltà) hanno ora più righe di scrittura (6 / 4 / 4). (2) **Scheda dell'opposizione**: pagina dedicata sul modello di quelle dei protagonisti, divisa nelle due parti per seme: ♦ Quadri · I nemici (box "Chi sono" + le due spinte ♦) e ♣ Fiori · Il caos (box "Che forma prende" + le due spinte ♣), più la nota sulle altre quattro spinte ♠♥. Le quattro domande-spinta sono le stesse di prima. Aggiornata anche la riga di sommario in cima al manuale (Appendice G elenca ora: schede dei protagonisti, scheda del viaggio, scheda dell'opposizione, carte dei Qualcuno). Solo manuale: nessun effetto su KB/regole/motore. PDF rigenerato.
- **Manuale: scheda "I Qualcuno" semplificata e testi introduttivi tolti dall'Appendice G/H** (luglio 2026): scelta di Saverio. (1) La scheda dei Qualcuno non usa più le sei mini-card 2×3 con l'etichetta "Qualcuno" ripetuta come titolo: ora sono otto box a tutta larghezza (nuova classe CSS `.sg-qrow` in `genera_pdf_manuale.py`), ciascuno con Nome e Carta affiancati in alto e Descrizione sotto; tolti l'occhiello e la nota "Come si usa" (restano solo i box). (2) Rimosso il paragrafo introduttivo dell'Appendice H ("Nove schede di riepilogo…"): resta solo il titolo. NB tecnico: la nota finale e il `</div>` di chiusura del foglio dell'opposizione erano andati persi lasciando il blocco HTML sbilanciato (+1), il che faceva "ingoiare" a Python-Markdown il titolo `# Appendice H` (reso come testo letterale, senza page-break); ripristinati, ora il titolo torna un vero `<h1>` su pagina propria. Solo manuale: nessun effetto su KB/regole/motore. PDF rigenerato.
- **Manuale: handout H.9 "Come narrare una mossa" con sotto-elenchi puntati** (luglio 2026): scelta di Saverio. I tre passi della scheda H.9 (Appendice H) non tengono più le voci in linea nel testo del `<li>`: ognuno ha ora un `<ul>` annidato con le voci indentate, come gli altri handout. Passo 1: le tre mosse (Hai introdotto? / Hai preso? / Hai fatto scopa?); Passo 2: i quattro semi (♠♥♦♣); Passo 3: i tre range di valore (Basso 1-3 / Medio 4-7 / Alto 8-10). Solo impaginazione dell'handout, testo invariato; nessun effetto su KB/regole/motore. PDF rigenerato.
- **Regole v1.45: costi delle figure a 3/6/9** (luglio 2026): scelta di Saverio. La Regina passa da 5 a 6 e il Re da 8 a 9 (il Fante resta a 3); i valori di presa (8/9/10) non cambiano. Obiettivo: rendere l'acquisto delle figure una scelta che pesa davvero. Con i vecchi 3/5/8 quasi tutti schieravano due figure (nelle simulazioni circa 1.8 per lato, bucket "2 figure" al 54%); con 3/6/9 la prima figura resta quasi garantita (Fante a 3, circa il 98% dei lati ne schiera almeno una) ma la seconda diventa una decisione (bucket "2" giù al 48%, media circa 1.64 per lato) e il Re resta il colpo raro. Si conserva l'invariante di design dei costi: Fante + Regina = 3 + 6 = 9 = Re (prima 3 + 5 = 8 = Re), quindi le due strade verso una figura da valore massimo costano ancora uguale. I punti netti di fine partita (Crescita) restano stabili per autoregolazione (figure più care si comprano meno, quindi si spende meno e avanza uguale), perciò gli scaglioni 0-2 / 3-6 / 7+ non vanno ritoccati. Propagato nello stesso commit a index.html (costante FIGURE, tabella e nota costi in REGOLE, bullet in REGOLE_IA, prompt del mercato di O), KB (§21 tabella, §16 nota costi, §20 esempio del resto riscritto sul Fante, registro, footer v1.45), manuale (§4.3, schede §5, handout H.2, footer), driver di test (partita_smart COSTO, label B.2 di benchmark) e benchmark ri-lanciato (storico + ultimo.md). NB: l'handout H.2 del manuale conteneva ancora gli EFFETTI datati pre-v1.43 di Fante/Regina (refuso preesistente dell'Appendice H): qui aggiornati i costi e corretti anche gli effetti (Fante e Regina allineati alla v1.43, testo dell'handout riscritto).
- **Hint di narrazione in-app ancorati alla posta** (luglio 2026): scelta di Saverio. La funzione `suggerimento()` in `index.html` (box di narrazione mostrato quando giochi una carta) non legava presa e scopa alla posta, a differenza dell'handout H.9 del manuale ("Come narrare una mossa"). Allineati: la presa ora recita "un successo che ti avvicina alla posta" e la scopa "spazzi via ogni ostacolo, ti avvicini alla posta e ottieni un vantaggio concreto", su entrambi i lati (per l'Opposizione "annienti ogni vantaggio dei protagonisti, ti avvicini alla posta..."). La presa a vuoto (figura sacrificata) resta senza l'aggancio alla posta, perché non avanza nulla. Solo testo degli hint: nessun effetto su meccaniche, motore o punteggio; l'H.9 e la funzione restano due copie a mano (vincolo file unico).
- **UI: la carta d'asta del vincitore sempre visibile accanto al piatto** (luglio 2026): richiesta di Saverio. Il vincitore dell'asta tiene la sua carta d'asta da parte e la gioca per ultima (`lato(win).astaCarta`); prima nell'app si vedeva solo nel momento in cui diventava l'ultima giocata. Ora un pannello `.asta-riservata` (tinta del lato) la mostra accanto al piatto in `rTurno` e nella vista spettatore online, per tutta la scena, così entrambi i lati la ricordano (la regola la vuole comunque scoperta: l'avversario la conosce già). Si nasconde quando è effettivamente il turno finale del vincitore (è già in mano, ramo `daAsta`). Implementazione: nuovo helper di solo render `astaRiservataHTML()` che legge lo stato senza mutarlo (rispetta il vincolo "le r*() non toccano G"); piatto e pannello avvolti in un `.tavolo-riga` flex, dimensioni in `calc(...*var(--k))`. Nessun effetto su meccaniche o motore. Verificato via jsdom (pannello presente nei turni, nascosto sull'ultima giocata); suite core verde.
